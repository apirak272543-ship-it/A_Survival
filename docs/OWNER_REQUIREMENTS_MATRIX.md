# A_Survival — Owner Requirements Matrix

เอกสารนี้เป็น source of truth ของคำสั่งจากการสนทนาทั้งหมดที่ทบทวนถึงวันที่ 27 สิงหาคม 2026 ใช้สถานะ `VERIFIED` เฉพาะรายการที่มี source/test/evidence รองรับจริง, `PARTIAL` เมื่อมีฐานบางส่วนแต่ acceptance ยังไม่ครบ และ `PENDING` เมื่อยังไม่มี implementation ที่ตรวจสอบได้ ห้ามสรุปว่างานเสร็จทั้งหมดตราบใดที่รายการใน scope ยังเป็น `PARTIAL` หรือ `PENDING`

## กติกาการทำงานและขอบเขต

| ID | ข้อกำหนดของเจ้าของ | หลักฐานปัจจุบัน | สถานะ | สิ่งที่ยังต้องผ่าน |
|---|---|---|---|---|
| O-01 | ต้องทำงานต่อจากคำสั่งทั้งหมดตั้งแต่เริ่มแชท ไม่ข้ามงานเก่า | matrix นี้สร้างจากบริบทที่ทบทวนแล้ว | PARTIAL | ตรวจ matrix ทุก checkpoint และเพิ่มแถวเมื่อมีคำสั่งใหม่ |
| O-02 | เล่นได้เฉพาะ Obsidian Frontier จนกว่า vertical slice จะเสร็จและได้รับอนุมัติ | `RUNTIME_MAP_ID`, direct-route tests, browser direct URL และ selector proof | VERIFIED | รักษา allow-list ต่อไปเมื่อเพิ่มระบบใหม่ |
| O-03 | ข้อมูล future maps เก็บเป็น planned/backend data ได้ แต่ห้ามมี playable runtime entry, cache preparation หรือ generator UI | selector/cache lookup จำกัดที่ `RUNTIME_MAP_ID`; browser map selector แสดงการ์ดเดียวและ direct map-002 fallback เข้า Obsidian | VERIFIED | คง future records เป็นข้อมูลหลังบ้านเท่านั้น และเพิ่ม guard เมื่อมี cache caller ใหม่ |
| O-04 | ถ้างานใดพึ่งเครื่องมือที่เจ้าของสั่ง ให้สร้าง/ตรวจเครื่องมือนั้นก่อน; หลังเกมเสถียรให้ใช้ Engine แทนการเขียนโค้ดตรง | มี Common Generator API/registry และ `cc7bc85` เพิ่ม generation-time `GeneratorAssetRef` gate: invalid SHA, missing reference-only provenance และ duplicate asset IDs fail closed ก่อน artifact reuse/save; PR #28 ยังแยกอ้าง tool inventory/dependency policy และยังไม่ merge | PARTIAL | สร้าง/ตรวจ tool inventory และ dependency policy ให้ครบทุก tool/domain, เชื่อม durable registry/orchestrator/export/review และยืนยันเครื่องมือใช้งานจริงโดยไม่เปิด player generator UI |
| O-05 | ทำทีละหน่วยที่จบ ตรวจจริง บันทึก matrix แล้ว commit/push ทันที ไม่รวมส่งทีเดียว | workflow นี้ใช้ checkpoint แยก; หน่วยแรกผ่าน test/check/build/browser แล้ว | PARTIAL | ต้องทำแบบเดียวกันกับทุกหน่วยถัดไป |

## ภาพ เกม และ mobile foundation

| ID | ข้อกำหนดของเจ้าของ | หลักฐานปัจจุบัน | สถานะ | สิ่งที่ยังต้องผ่าน |
|---|---|---|---|---|
| V-01 | ภาพเป็น original crisp voxel/pixel RPG มี relief, mountains, rocks, paths และอ่านวัตถุจริงได้ | generated voxel pack, rolling terrain และ Babylon scene ใน GitHub main | PARTIAL | ปรับความหนาแน่น/ความมืด/ความต่างสี และตรวจภาพจริงทุก camera mode |
| V-02 | ไม่ใช้พื้นแบน RGB/glow จนกลืน player, pet, enemy | terrain material และ scene lighting มีฐานแล้ว | PARTIAL | visual pass และ screenshot acceptance |
| V-03 | ตัวละคร สัตว์เลี้ยง มอนสเตอร์ item ฉาก ต้นไม้ และ texture แยกเป็น mod/texture pack replaceable | asset pack manifest และ loader ใน source | PARTIAL | ทุก runtime asset ต้อง resolve จาก manifest/assetId/SHA/provenance |
| V-04 | external/community asset ต้องมี license/provenance/เครดิต และ reference-only ห้ามเป็น runtime | `ASSET_CREDITS`, active manifest/provenance graph, `creditsPresentationContract.ts` และ player `CreditsSheet` แยก project-original/license-verified/reference-only/needs-review พร้อม source URL; license approval/owner-contact/publish workflow ยังไม่มี | PARTIAL | ตรวจ license/owner contact ของ runtime assets ให้ครบ, ทำ approval/registry workflow และ publish gate โดย reference-only ต้องไม่เข้า runtime |
| M-01 | เล่นบนมือถือแนวนอนจริง ไม่ล้นจอ มี safe area, fullscreen และ touch controls | `mobileViewportPolicy.ts` มี pure normalization ของ viewport/orientation/safe-area/touch/fullscreen; `index.css` มี safe-area fallback variables และ actual landscape sizing/overflow guards สำหรับ HUD, controls, settings, vault และ footer; Chromium viewport evidence 320/390/430/768 ผ่านแบบ sandbox | PARTIAL | ทำ device/WebView acceptance จริง, ตรวจ fullscreen/touch/notch/navigation bar บนอุปกรณ์จริง และไม่อ้าง browser viewport เป็น device proof |
| M-02 | render/load เฉพาะพื้นที่ใกล้ผู้เล่นและปรับตามสเปกมือถือ | visible-region pool และ render-distance presets | PARTIAL | benchmark FPS/memory/draw calls บนมือถือจริง |
| M-03 | offline-first, cache, integrity และเล่นต่อเมื่อ network หาย | `client/src/game/storage/mapCache.ts` ตรวจ Cache Storage payload ให้ตรง canonical map id/name/key art/content และมี cached timestamp ก่อนถือว่า cache ใช้ได้; stale/corrupt payload fail-closed และ future-map preparation/lookup ถูกปฏิเสธ; `server/mapCache.test.ts` มี focused cache/offline/future-map regression 5 tests; service-worker boundary ยังอยู่ใน PR #46 ที่ยังไม่ merge | PARTIAL | service-worker integration, cached digest/version migration, airplane-mode/reconnect/resync, map/chest/plant persistence จริง และ real-device/WebView acceptance |

## Block-first และ world interaction

| ID | ข้อกำหนดของเจ้าของ | หลักฐานปัจจุบัน | สถานะ | สิ่งที่ยังต้องผ่าน |
|---|---|---|---|---|
| B-01 | สิ่งที่เป็นบล็อกได้ต้องเป็น independent coordinate/state/action; ต้นไม้/ใบไม้เป็น block records | Obsidian slice มี deterministic block coordinates, per-cell mesh metadata, override state และ action resolver สำหรับ slab/player block; `generateBlockGroup` ใช้ canonical `addIfEmpty`/`blockKey` ทำให้ duplicate rounded coordinates เป็น first-wins และ output มี unique keys; `createWorldBlock` ปัด x/y/z ครั้งเดียวก่อนเขียนทั้ง fields และ key เพื่อไม่ให้ record coordinate กับ key ขัดกัน; tree/leaf generators ใช้ guard เดียวกัน; pure B-01 graph PR #44/#49 ยังไม่ merge | PARTIAL | ขยาย block records ให้ครอบคลุม world content/tree-leaf families และ per-block mutation ทุกชนิด, เชื่อม authoritative persistence/inventory/physics และทำ browser/device acceptance |
| B-02 | partial/non-solid object นับเป็น 1 cell แต่เดินผ่านได้ตามชนิด; hazard และ cactus/หนามทำ damage | `blockPhysicsSystem.ts` ใช้ canonical `BlockDefinition` shape bounds: solid slab เป็น partial blocking, thin non-solid plant/cactus ไม่ block แต่ cactus รายงาน hazard damage/cooldown; `scene.ts` ใช้ blocking/hazard contacts ใน Obsidian runtime; focused physics tests ครอบคลุม occupancy, pass-through, hazard และ cooldown | PARTIAL | ขยาย partial/occupancy/hazard coverage ให้ครบทุก world block family และทำ deterministic gameplay/device acceptance; ยังไม่มี universal gravity/physics owner หรือ real-device evidence |
| B-03 | support/gravity: บางบล็อกวางต่อจากเพื่อนได้ บางชนิดลอยได้ บางชนิดต้องตกเมื่อฐานหาย | Obsidian slice มี solid-support gate, empty-cell gate, partial/solid flags และ unsupported-gravity query ใน `blockPhysicsSystem.ts`; focused runtime tests และ merged pure `blockSupportGravityDependencyGraph.ts` audit canonical definitions/6-direction support, broken exclusion, callback, gravity/float consistency | PARTIAL | universal support/gravity/partial occupancy validator, runtime mutation/reload coverage ทุก generated-world family และ device acceptance |
| B-04 | generic/wrong tool ทำลายโดยไม่คืน item; correct tool คืน placeable block item | Obsidian slab ใช้ pickaxe แล้วคืน `structure-001` พร้อม drop provenance; hand/wrong tool ทำลายโดยไม่คืน block; `blockDropToolDependencyGraph.ts` เพิ่ม pure audit ของ canonical block-item links, correct/wrong tool behavior, quantity/stack และ fail-closed durability blocker | PARTIAL | ขยาย tool/block registry ให้ครอบคลุม block families, เชื่อม authoritative durability/inventory mutation และทำ browser/reload/cross-map/device acceptance |
| B-05 | วางบล็อกและคง state ข้าม reload/map ตามกฎ | `OfflineMapState` มี `worldBlockOverrides`, composite key `[mapId+playerId]`, runtime rehydration และ browser IndexedDB proof ของ break/place หลัง reload; `blockPersistenceDependencyGraph.ts` เพิ่ม pure audit ของ coordinate normalization, tombstone/replacement, namespace และ Obsidian-only write gate; `3c1d4d8` hardens `normalizeWorldBlockOverrideMap` ให้เก็บเฉพาะ integer coordinate keys ที่อ้าง canonical `BlockDefinition` ได้หรือเป็น null tombstone และตัด unknown/malformed values ก่อน state save/load | PARTIAL | เชื่อม/ทดสอบ owner runtime แบบ end-to-end, browser IndexedDB reload/reconnect, cross-device conflict, production persistence และ universal world state; ยังไม่มี digest/version migration หรือ real-device acceptance |
| B-06 | carry inventory 40 ช่อง, block stack ปกติ 64 และ item-specific caps; carry ข้าม map | `CARRY_SLOT_LIMIT=40` ถูกใช้เป็น transfer gate, UI แสดง carry `/40`, block stack cap 64 และ tests ปฏิเสธการ withdraw เมื่อ carry เต็ม; merged `inventoryCapacityDependencyGraph.ts` อ่าน canonical `PLAYER_INVENTORY_SLOTS=40`, `WORLD_STORAGE_DEFAULT_SLOTS=27` และ block definitions, ตรวจ same-definition merge/overflow remainder/non-stackable full-capacity แบบ pure preview และตั้ง `cross-map-carry-owner-missing` เป็น required blocker; AI-0 implementation `ee2126e` เพิ่ม fail-closed guard ใน `addItemToContainer` สำหรับ quantity ที่ไม่ finite/ไม่เป็นจำนวนเต็ม/ไม่เป็นบวก และ capacity ที่ไม่ถูกต้อง โดย full `128` files / `547` tests และ `pnpm check` ผ่าน; production build ถูกยุติด้วย exit `143` จาก memory pressure จึงไม่อ้าง build ผ่าน | PARTIAL | enforce/test exactly 40 carry slots ทั่วทุก reward/action, authoritative overflow/stack merge, profile/session cross-map carry และ persistence/reload evidence |
| B-07 | chest แยกจาก carry เป็น storage map-local | `worldStorageSystem.ts` บังคับ chest 27 ช่องและ atomic transfer พร้อม provenance; `OfflineMapState.worldStorageById` อยู่ใน composite `[mapId+playerId]`; ChestSheet เปิดจาก deterministic Obsidian chest และ browser deposit/withdraw/reload proof ผ่าน; merged `worldStorageIsolationDependencyGraph.ts` เพิ่ม pure audit ของ separate chest/carry, map-scoped transfer action, duplicate normalization, canonical Obsidian anchor และ future-map fail-closed guard | PARTIAL | universal world-state integration, full cross-map storage contract, authenticated sync/conflict, production persistence และ device acceptance |

## Backend generation และ content registry

| ID | ข้อกำหนดของเจ้าของ | หลักฐานปัจจุบัน | สถานะ | สิ่งที่ยังต้องผ่าน |
|---|---|---|---|---|
| G-01 | World Generator เป็น backend-only, deterministic และ generate terrain/water/trees/structures/NPC/boss/safe zone/shop ครบตามกฎ | current canonical `tools/world-generator.ts` มี deterministic Obsidian generator; merged `worldGeneratorCoverageDependencyGraph.ts` เรียก `generateWorld` แบบ in-memory และตรวจ terrain/water/tree/vegetation/resources/caves/safe-zone/shop/npc-camp/ruin/boss-room รวม regular/animal/NPC/boss spawn roles, repeat hash, spatial validation และ no-write module export preview; future-map input fail-closed และ runtime/player/cache policy ปิด | PARTIAL | universal/world-wide output coverage, persisted module export/asset binding, live NPC/boss behavior, runtime loading และ no-player-generator browser/device acceptance |
| G-02 | hard spatial rules: bounds, surface, height layers, slope, water, overlap, clearance, support/gravity และ repair-before-export | `tools/worldSpatialConstraints.ts` มี real Obsidian bounds/surface/height/slope/water/overlap/grounding/support rules และ repair; `worldSpatialDependencyGraph.ts` เชื่อม generator กับ `validateGeneratedWorld`, `repairGeneratedWorld`, placement assessment และ bounded tests; `c4892d1` เพิ่ม fail-closed guard ให้ non-array, unknown และ non-string placement subjects ถูกปฏิเสธก่อน generate. Full `128` files / `550` tests, `pnpm check` และ `git diff --check` ผ่าน | PARTIAL | ขยายจาก Obsidian bounded slice ให้เป็น universal/world-wide coverage และเพิ่ม exported artifact acceptance; ยังไม่มี player generator UI หรือ runtime future-map write |
| G-03 | ไม่มี generator/content editor/player generator UI ในเกม | current UI ไม่มี generator button | VERIFIED | รักษา guard ต่อไป |
| G-04 | reusable Content Generation Suite แยก Definition/Model/Texture/Skin/Variant/Gameplay พร้อม semantic visual design, override, decision log, cache/provenance | Common Generator API และ plugins สำหรับ structure/content catalog/item/texture มี artifact schema, deterministic hash, preview, save/export, asset refs และ provenance; `server/creatorRouter.ts` มี admin-only texture validate/build/generate/preview; suite รายด้าน/ durable registry ยังไม่ครบ | PARTIAL | เพิ่ม generator รายด้าน, durable content registry/orchestrator, model/variant/gameplay contracts และ no-code engine |
| G-05 | assets ต้อง manifest/assetId/SHA/provenance-backed; unknown-license เป็น reference-only | current gameplay pack มี manifest/loader และ Builder output มี per-entry SHA-256, logical IDs, manifest hash, source/provenance; migrated future pack `a-survival-content-library-builder-v0-1` ผ่าน migration tests แต่ยังไม่ใช่ runtime registry ทั้งหมด | PARTIAL | ตรวจทุก runtime asset, durable registry และ Credits UI |
| G-06 | optional AI NPC หนึ่งตัวต่อ map, server-only/on-demand/default disabled, max one และ safe bounded action | canonical `server/aiNpcService.ts` และ `server/routers.ts` มี Gemini provider call ผ่าน server-side public tRPC mutation, allow-list เฉพาะ `obsidian-frontier:special-ai`, default disabled เมื่อไม่มี `AI_NPC_ENABLED=true` และ server key, cooldown/max-turn/message/timeout bounds, structured output validation, local action bounds และ deterministic fallback; `738f01f` เพิ่ม timeout cleanup ในทุก success/error/abort path พร้อม regression test | PARTIAL | ยังไม่มี in-game NPC caller/encounter binding, provider availability/production secret configuration, authoritative action execution, rate limit/auth abuse policy, background/queue integration, persistence และ device/production acceptance; ไม่อ้างว่าเป็น live AI NPC ครบระบบ |

## Plants, farming และ ecology

| ID | ข้อกำหนดของเจ้าของ | หลักฐานปัจจุบัน | สถานะ | สิ่งที่ยังต้องผ่าน |
|---|---|---|---|---|
| F-01 | มี plant catalog ประมาณ 300 ชนิด กระจายตาม biome/soil และเพิ่มได้ด้วย data | `PLANT_CATALOG` และ `PLANT_ITEMS` มี 300 runtime records; world-farm adapter ใช้ runtime plant/seed/yield source เดียวกัน, รองรับ legacy seed alias และมี canonical relationship tests | PARTIAL | ตรวจ distribution ให้ครอบคลุม biome/soil ตาม design และผูก asset coverage ครบทุก plant |
| F-02 | พืชผูกกับ soil/block/biome; ปลูก เติบโต เก็บเกี่ยวได้ | Obsidian world farm มี plot/soil/biome gate, planting, growth state และ harvest plan พร้อม tests/browser proof; `c777051` เพิ่ม normalization guard ให้ seedDefinitionId ต้อง resolve กลับไปยัง plant canonical link เดียวกัน และ full `128` files / `552` tests กับ `pnpm check` ผ่าน | PARTIAL | ขยาย soil/block/biome coverage และ world distribution ให้ครบทุก plant family พร้อม authoritative persistence |
| F-03 | stage ของพืชเป็น seed/sprout/young/mature และ mature-only harvest/reward | world farm stages, elapsed-time rehydration, mature-only harvest, reward provenance และ cleared plot ผ่าน focused tests กับ browser proof; `ee5bbbe` แก้ `harvestWorldPlant` ไม่ให้ concat reward ข้าม `PLAYER_INVENTORY_SLOTS=40` แต่ใช้ `addItemToContainer` แบบ atomic จึงไม่ล้าง mature plot เมื่อ carry เต็มและเคารพ stack rules | PARTIAL | เพิ่ม stage/profile coverage, authoritative harvest persistence/reconnect, unloaded-chunk abstraction ระยะยาว และ browser/device acceptance |
| F-04 | ผลกระทบ healing/buff/repel/damage เป็น fictional, capped และไม่ทำ medical claim; cactus มี thorn damage | runtime plant effects ถูกแปลงเข้า world-farm view เฉพาะ healing/repellent และ cap แล้ว; `83252da` เพิ่ม fail-closed validation ต่อ non-finite, fractional, negative, over-cap, invalid-duration และ blank-label values; tests ยืนยัน mature-only/capped/non-lethal behavior; cactus thorn และ universal effect engine ยังไม่มี | PARTIAL | เพิ่ม cactus/plant hazard และ universal target/effect disclosure รวมถึง runtime application |
| F-05 | พืช repellent ผลักมอนสเตอร์ใกล้บ้าน/พื้นที่ ไม่ฆ่าอัตโนมัติ และมี radius/stacking/duration rules | `83252da` เพิ่ม validator ที่บังคับ radius finite/ไม่ติดลบ/ไม่เกิน 6, duration เป็น positive integer, `stackable=false` และ label ไม่ว่าง; focused/full tests ผ่าน แต่ browser proof กับศัตรูและ runtime application ยังไม่ครบ | PARTIAL | ยืนยัน browser behavior พร้อม enemy ใกล้เคียง และเพิ่ม runtime effect application/stacking/duration coverage |
| F-06 | seed/plant ที่เก็บจากโลกใช้ปลูกต่อและ soil ที่เหมาะสม | planting consume เป็น atomic และ harvest reward มี map provenance; `c777051` ปฏิเสธ persisted seed/plant link ที่ไม่ตรงกันระหว่าง normalization แต่ยังไม่ได้เพิ่ม reward/drop ใหม่ | PARTIAL | เพิ่ม seed/plant drop chain และปลูกต่อครบวงจร |
| F-07 | Universal Plant/Tree/Ecology/Farm Engine ครอบคลุม environment, nutrients, fertilizer, health/stress/disease, pests, pollination, seasons/weather, lifecycle, genetics, automation, processing/trade | `plantEcologyPolicy.ts` เป็น pure data-driven policy จาก `PLANT_CATALOG` ครบ soil/biome/lifecycle และ bounded fictional factors; runtime owners ของ nutrients/pests/seasons และ full ecology engine ยังไม่มี | PARTIAL | เพิ่ม runtime ecology owners, nutrient/fertilizer/health/pest/pollination/season/genetics/automation/processing และเชื่อมกับ `worldFarmSystem.ts` หลังมี acceptance ใหม่ |

## Camera, settings และ performance

| ID | ข้อกำหนดของเจ้าของ | หลักฐานปัจจุบัน | สถานะ | สิ่งที่ยังต้องผ่าน |
|---|---|---|---|---|
| S-01 | ผู้เล่นเลือก first-person/build-farm, overhead/action หรือ side/over-shoulder เองใน In-map Settings | `InMapSettingsSheet` มีตัวเลือกบุคคลที่ 1/มุมสูง/มุมด้านข้าง; scene ใช้ UniversalCamera + ArcRotate camera; browser proof เปลี่ยน first-person, side และ overhead จริง; ค่า hydrate หลัง reload; `381a490` เพิ่ม finite-input guard ใน `cameraRelativeMovement`; `6bfb0c9` ทำให้ invalid legacy fallback กลับ overhead; full `128` files / `552` tests กับ `pnpm check` ผ่าน | PARTIAL | ตรวจ touch/collision และ device sizes เพิ่ม; เก็บภาพ acceptance ให้ครบตามอุปกรณ์เป้าหมาย |
| S-02 | แยก Outside/Global Settings จาก In-map Settings; global language/graphics/audio/defaults, in-map view/control/pause | Global sheet ระบุ `Global settings · App-wide` และเก็บภาพ/เสียง/การควบคุม; In-map sheet อยู่เฉพาะ `screen === game` และบันทึก camera/view/FPS ด้วย `[mapId+playerId]`; offline normalization tests + browser reload proof | PARTIAL | เพิ่ม global/in-map acceptance ทุก entry route และตรวจ pause/focus behavior ให้ครบ |
| S-03 | view distance steps 5,10,...50 blocks และ target FPS 5..60 + 120 แบบไม่รับประกัน | `renderDistance.ts` มี view distance 5–50 ทีละ 5 และ `TARGET_FPS_OPTIONS` ครบ 5–60 ทีละ 5 พร้อม 120; `performanceProfile.ts` มี tier budget และ `GameCanvas` ใช้ effective target-FPS ceiling ใน render loop; in-map settings ระบุทุกค่าเป็น target และค่า 120 เป็น `high-refresh advisory · ไม่รับประกัน`; browser evidence เลือก 120 และเห็น disclaimer/note จริง | PARTIAL | เพิ่ม fine-grained streaming/LOD policy ให้ครบทุกค่าหากจำเป็น และทำ device/FPS evidence จริง; ยังไม่อ้าง benchmark หรือรับประกันอุปกรณ์ |
| S-04 | adaptive performance: device tiers, WebGL capability, LOD, culling, pooling, hysteresis, sleep/wake, distance-based updates | explicit persisted `low`/`balanced`/`high` profile policy ใน `performanceProfile.ts`; tier budgets clamp view/FPS/mob/animation/physics/particles/shadows/LOD; scene ใช้ view metadata, distance-based plant/enemy updates, enemy sleep/wake และ tier-aware spatial activation ของ world/farm/storage meshes; bounded telemetry sampler รายงาน cadence/mesh metadata แบบ QA-only; merged `adaptivePerformanceDependencyGraph.ts` เชื่อม capability advice, static budgets, render-distance/visibility, telemetry และ preview-only profiler พร้อม required blockers สำหรับ adaptive controller, runtime LOD, hysteresis, pooling และ sleep/wake; focused graph 5 tests ผ่าน | PARTIAL | เพิ่ม WebGL/device capability detection ที่เชื่อถือได้, hysteresis/auto-adaptive controller, unified frustum/occlusion culling, pooling/LOD runtime implementation, caller integration และ real-device benchmark; ห้ามตีความเป็น mobile FPS acceptance |

## Codex, credits, language และ rating

| ID | ข้อกำหนดของเจ้าของ | หลักฐานปัจจุบัน | สถานะ | สิ่งที่ยังต้องผ่าน |
|---|---|---|---|---|
| C-01 | Codex รวม item ทุกประเภทแต่แสดงเฉพาะสิ่งที่เคยเก็บ/ค้นพบแล้ว มีหมวดและ detail | `CODEX_ENTRIES` และ `codexDiscoveryContract.ts` มี discovered-only normalization, canonical ordering, empty state, duplicate/unknown rejection และ category filtering; `ArcaneFrontier.tsx` ใช้ `createCodexDiscoverySnapshot` ให้ CodexSheet แสดงเฉพาะ canonical discovered entries และซ่อนรายการทั้งหมดเมื่อ input discovery ไม่ valid; browser เปิด Codex จาก player landing ได้จริง | PARTIAL | เชื่อม discovery event กับ owner ที่ authoritative, durable session/IndexedDB persistence และทำ browser/mobile acceptance เพิ่ม |
| C-02 | item detail แสดงคุณสมบัติ damage/plant/stack/usage แบบไม่รก | Vault มี long-press 3.5 วินาทีเปิด `ItemDetailSheet`; sheet ใช้ `getItemCategoryDetail` แสดง usage/stack และ category facts ของ plant/block/tool จาก canonical definitions; weapon attack damage แสดง unavailable พร้อม reason ไม่สร้างตัวเลข | PARTIAL | เพิ่ม UI/mobile acceptance สำหรับทุก category และเพิ่ม authoritative combat damage owner ก่อนแสดงตัวเลข |
| C-03 | Credits/Supporters ซ่อนไว้ใน UI แรก กดเข้าไปดูได้ แยก runtime/reference-only | `ArcaneFrontier.tsx` มี CreditsSheet และปุ่ม `เครดิต` จาก landing/lobby; CreditsSheet ใช้ `createCreditsPresentation(ASSET_CREDITS)` แสดง canonical project/licensed/reference-only/needs-review sections และ fail-closed เมื่อ contract input ไม่ valid; browser เปิดจาก player landing ได้จริงและเห็น reference-only link/label | PARTIAL | ทำ browser/mobile acceptance เพิ่ม, คงการแยก reference-only ออกจาก runtime distribution และเพิ่ม durable registry/license approval ก่อน publish ใด ๆ |
| L-01 | ภาษาไทยเป็นค่าเริ่มต้น ใช้ภาษาชีวิตประจำวัน ไม่ราชาศัพท์/ไม่สวยหรูเกินไป | current UI ผสมไทย/อังกฤษ | PARTIAL | copy pass ทุก screen และ resource จริงก่อนเปิด English |
| L-02 | เกมไม่ทำสำหรับเด็ก; colloquial/profane dialogue/voice ได้ตามบริบท แต่ system/safety copy ต้องชัด | ยังไม่มี policy/rating/voice source ใน current GitHub main | PENDING | policy, labels, voice provenance และ review |

## เครื่องมือ deferred และปลายทาง no-code

| ID | ข้อกำหนดของเจ้าของ | หลักฐานปัจจุบัน | สถานะ | สิ่งที่ยังต้องผ่าน |
|---|---|---|---|---|
| T-01 | Runtime rendering/performance tool ใช้ generate once → pre-generated data → registry/cache → chunk/culling/LOD/pooling | `performanceProfile.ts` เป็น data-driven runtime budget layer; `runtimePerformanceCapability.ts` pure one-time capability advisory contract; `runtimePerformanceTelemetry.ts` bounded sampler และ `runtimeVisibilitySystem.ts` pure distance activation policy; `GameCanvas` clamp cadence, snapshot มี rendered/throttled frames, avg/p95/worst cadence และ total/active mesh metadata เฉพาะ QA DOM; `runtimePerformanceProfiler.ts` และ `CreatorDomainWorkbench` มี admin-only Thai preview วิเคราะห์ snapshot; focused/full tests, build และ browser player/creator boundary evidence ผ่าน แต่ยังไม่มี GPU/device benchmark, full registry-backed LOD/occlusion/pooling หรือ durable profiler history | PARTIAL | เชื่อม profiler กับ controlled capture/export และ asset registry/cache/culling/LOD/pooling ให้ครบ โดยไม่ generate ใน render loop และไม่เปิด profiler ให้ player |
| T-02 | Procedural Animation/Motion Generator มี library, skeleton, profiles, seeded variation, wind, retargeting, animation LOD | `server/generators/animationProfileGenerator.ts` และ CLI/creator preview สร้าง profile 7 states, asset provenance, deterministic hash และ runtime reuse/sleep policy; `client/src/game/systems/animationMotionPolicy.ts` เพิ่ม pure full/reduced/sleep/static decision ตาม visibility, distance, tier และ reduced-motion; merged `animationMotionDependencyGraph.ts` เชื่อม profile/policy/provenance แบบ read-only และตั้ง required blockers สำหรับ runtime caller/variation, skeleton retarget, wind และ binary asset generation | PARTIAL | เพิ่ม motion library/skeleton/variation/wind/retargeting, binary asset coverage และเชื่อม runtime caller หลังมี asset clips จริง; ยังไม่อ้าง device/visual acceptance |
| T-03 | Universal Item/Equipment/Combat/Crafting/Assembly Logic มี relationship, weapon/tool families, damage/status/elements, synergy, durability, progression, economy | current catalog/combat เป็นฐานบางส่วน; `server/generators/contentCatalogGenerator.ts` สร้าง metadata catalog 10 categories ขั้นต่ำ 300 ต่อหมวด และ `server/generators/universalItemEngine.ts` มี item identity/role/material/resource/progression, power budget, trade-off, counter, effects/stack cap, compatibility, repair profile และ balance validation; `92bde2e` เพิ่ม fail-closed checks สำหรับ max budget และ numeric payload ของ stat/durability/effect/repair/resource/trade-off/performance โดย full `128` files / `552` tests และ `pnpm check` ผ่าน | PARTIAL | เชื่อม crafting/equipment/combat transactions, armor/set/enchant/socket/upgrade, economy/world relationship และ tool-aware runtime |
| T-04 | Common Generator API / Game Creation Engine ครอบคลุม world/quest/structure/dungeon/crafting/economy/audio/weather/vegetation/farming/test/profiling/migration | มี `server/generators/commonGeneratorApi.ts` และ plugins `structure.placement`, `content.catalog`, `item.universal`, `texture.pack`, `animation.profile`, `creator.profiler.preview` และ `creator.artifact.preview/register/list/review/audit/export/compatibility`; มี protected creator previews, additive `creatorDomainArtifacts` schema/migrations, deterministic metadata hash/provenance/review/audit transition, approved-only metadata export preview, strict Obsidian-only compatibility validator, texture artifact registry พร้อม texture review/list/audit owner, composition-to-texture transient preview adapter, pure dependency graph validator (`a-survival.dependency-graph.v1`), actual content-catalog→asset→sampled-definition graph adapter, quest→catalog reference adapter และ Thai Workbench inspector; `assetProvenanceDependencyGraph.ts` เชื่อม sampled item/plant asset IDs กับ active manifest และ credits แบบ read-only พร้อม required blockers; generator รายด้านอื่น, graph orchestrator ที่ดึง artifact จริงจากทุก domain, production DB rollout, asset export และ runtime publish ยังไม่ครบ | PARTIAL | เชื่อม plugin/tool รายด้านทีละรายการ, ขยาย durable registry/orchestrator/profiler export, DB rollout/review audit, asset compatibility/export และคง no player generator UI |
| T-05 | งานสุดท้ายเป็น no-code ภาษาไทยแบบลากวาง/ถูวาง/ประกอบเหมือน LEGO และ pixel/mob editor เลือกสัดส่วน สี เฉด และสไตล์ แล้ว validate/register/export เข้าเกม | `CreatorStudio` และ `CreatorDomainWorkbench` เป็น developer-only Thai routes แยกจาก player; ใช้ shared `client/src/lib/creatorTemplateCatalog.ts` เป็น source-of-truth ของ template/skin-part catalog; รองรับ pixel template/layers/palette/provenance, bounded 32×32 click-to-paint pixel grid พร้อม layer selector, template-driven subject/canvas/part selection และล้าง pixel draft เมื่อเปลี่ยน preset, no-code composition manifest ของ canvas/part/slot/layer mapping, transient composition→existing texture.pack preview ที่ flatten ตาม z-index/visibility/opacity และผูก composition SHA/provenance, animation→skin texture/skinLayout validation, admin-only texture export preview ที่คืน PNG bytes + deterministic `manifest.json` sidecar/hash/download name และ ZIP package เมื่อผู้ดูแลกดเอง, admin-only byte compatibility preview ที่ตรวจ manifest/PNG/ZIP hash/file list/CRC ด้วย reason codes, explicit composition register ที่ preflight ผ่าน verified helper แล้ว reuse durable texture-pack registry owner, texture-pack draft/approve/reject/reopen review/list/audit workflow with immutable events, bounded canonical multi-part composition selector สำหรับ animation skin, world/structure/item/weapon/block/animation/quest preview, profiler snapshot preview, structured generic artifact metadata preview/register/list, generic draft/approve/reject/reopen review controls, immutable review history for domain artifacts, approved-only metadata export preview และ strict target-map compatibility preview, dependency graph preview ที่ generate `content.catalog` artifact จริงจาก seed, ตรวจ asset/definition references และต่อ `quest.progression` sampled objective/reward/prerequisite references, required reference/kind/generator/version/hash/cycle พร้อม seed/rules metadata; creator routes ใช้ server-side creator guard ที่รองรับ `gm`/`admin`/`master`; browser ยืนยัน player route ไม่มี composition/registry/review/export/compatibility/profiler/dependency graph text/panel และ unauthenticated creator route ถูก gate; authority-specific role boundary มี `master`-only management route และ immutable audit contract แล้ว แต่ยังไม่มี texture atlas/actual model bytes ถาวร, skeleton/rig, durable downloadable pack storage, multi-asset domain package policy, generic runtime publish workflow, model attachment หรือ runtime publish | PARTIAL | เพิ่ม creator-specific authorization, pixel atlas/texture pack export แบบ durable, durable artifact/manifest/asset storage สำหรับ domain อื่น, multi-asset package policy, DB/storage E2E และ migration approval, register/download/export workflow ที่ผูก generator outputครบทุก domain, binary/model compatibility checks สำหรับ durable assets, review E2E บน registry จริง, per-part paint lock/viewport, texture atlas/actual model bytes, publish approval/report และเปิด route ตาม developer permission |
| T-06 | Structure/Building Generator: Generate Once → Asset/Blueprint → Placement Rules → Generation Rules → Validator/Registry; รองรับ object/building/compound/settlement/landmark, biome/terrain/climate/road/space/overlap rules, interiors, decorations และ mob/NPC linkage | มี `server/generators/structureGenerator.ts` เป็น backend/data tool พร้อม blueprint 5 levels, deterministic seeded placement, score 0–100, bounds/ground repair, reject-before-export, overlap avoidance/fallback, NPC/mob/interior/child rules และ assetRefs; ยังไม่มี actual structure asset pack, terrain/road/interior solver หรือ runtime integration | PARTIAL | เพิ่ม reusable blueprint library/asset packs, road/interior systems, world-instance integration และ keep no player generator UI |
| T-07 | Story/Quest/Map Progression เชื่อม map 1–100, map 1–10 เปิดเล่นได้, map 11+ gated ด้วย quest chain, อย่างน้อย 20 quests ต่อ map, rewards/abilities/items/weapons สัมพันธ์กับโลก; item detail กดค้าง 3–5 วินาทีและ short detail เมื่อสลับของ | `quest.progression` สร้าง 100 map/2,000 quests และตรวจ sequential gate, unique title/story beat, reward และ future-map runtime lock; `storyProgressionSystem` เก็บ progress local-first เฉพาะ Obsidian chain; Codex/Credits เดิมยังคง discovered/provenance rules; player browser evidence ยืนยัน long-press 3.6s เปิด full item detail และ hotbar selection แสดง short detail; `questGameplayEventDependencyGraph` ต่อ objective ของ MAP_001 กับ action owner/payload contracts แบบ read-only และแสดง missing event/target blockers; `questRewardRuntimeDependencyGraph` ตรวจ reward item/ability กับ catalog, reward-instance factory และ reward dispatch owner แบบ read-only; `questRewardInventoryDependencyGraph` ต่อ catalog reward-instance factory กับ `addItemToContainer` แบบ dry-run เพื่อตรวจ 40-slot capacity และคง missing dispatch/ability blockers; `questRewardDispatchSystem` เป็น pure atomic transition สำหรับ item-only reward, fail closed เมื่อพบ reputation/ability ที่ไม่มี owner และตั้งแต่ `1e1b177` ปฏิเสธ explicit non-finite/non-integer/non-positive/เกิน item stack quantity ก่อนสร้าง reward instance; `questRewardPendingAction.ts` แปลงเฉพาะ accepted pure result เป็น JSON-safe bounded `HomeAction` พร้อม deterministic one-to-one event/instance provenance และ `isSafeQuestRewardDispatchPayload` ตรวจ Obsidian/MAP_001, semantic `questId`↔`questOrder` และ ID bounds; `indexedDb.ts` มี fail-closed queue gate สำหรับ action ชนิดนี้ และ `writeGameSyncBatch` ใช้ server allow-list ผ่าน `isSafeSyncAction`; pending action ยังไม่ถูกเรียกจาก gameplay และยังไม่มี authoritative reward completion/ability/reputation owner | PARTIAL | เติม canonical event/target binding สำหรับ objective ทุกชนิด, authoritative reward/ability transaction, map unlock persistence ที่ไม่เปิด future runtime ก่อน acceptance, abuse/idempotency/auth policy และตัดสินใจ UX dialogue/cutscene |

## Validation และ reporting rules

| ID | ข้อกำหนดของเจ้าของ | หลักฐานปัจจุบัน | สถานะ | สิ่งที่ยังต้องผ่าน |
|---|---|---|---|---|
| Q-01 | ใช้ Google/Gemini เมื่อไม่รู้ โดยบันทึก source และไม่อ้าง call ที่ล้มเหลว | research notes อยู่ในบริบทเดิม แต่ไม่อยู่ใน current repo อย่างครบ | PARTIAL | เก็บ research/audit files ต่อ checkpoint |
| Q-02 | ศึกษา Minecraft/เกมอื่นเป็น reference แต่ห้าม copy code/assets/branding | `docs/research/bedrock-reference-notes-2026-08-28.md` บันทึก Microsoft Learn Bedrock item/block listings และ Minecraft Wiki item semantics; กำหนดให้ใช้ gameplay/category/state patterns เป็น reference architecture แต่ใช้ IDs, names, visuals, mechanics details และ provenance ของ A_Survival เอง | PARTIAL | เพิ่ม source/counting policy ที่กำหนด universe ชัดเจน, ตรวจ asset provenance รายรายการ และทำ original visual/content implementation |
| Q-03 | ต้องรัน test/check/build/browser evidence ก่อน report | Phase 6 camera checkpoint รัน `40 files / 130 tests`, `pnpm check`, production build และ browser evidence ใน `docs/phase6-browser-notes.md`; direct future-map URL ยัง fallback เข้า Obsidian | PARTIAL | ทำ validation แบบเดียวกันทุกหน่วยถัดไป และเติม mobile/device evidence เมื่อทำได้ |
| Q-04 | ต้องระบุข้อจำกัดจริงและห้ามใช้คำว่าเสร็จเมื่อยังมี PARTIAL/PENDING ใน current scope | กฎนี้เป็น reporting contract | VERIFIED | รักษาในทุกรายงาน |

## Checkpoint log

| วันที่ | หน่วยงาน | ผลตรวจ | Commit/GitHub | หมายเหตุ |
|---|---|---|---|---|
| 2026-08-26 | forensic reconciliation | พบ local branch `e6ba26c` ไม่ใช่ GitHub main; GitHub main ปัจจุบัน `0a9586f` ไม่มี later farming/chest/camera/matrix source ที่เคยอ้างใน checkpoint ก่อนหน้า | ยังไม่ push งาน repair | สร้าง local backup ref `local-pre-forensic-e6ba` และ stash `forensic-preserve-map-cache-v3`; ห้ามถือ audit เก่าเป็น source code |
| 2026-08-26 | Obsidian-only runtime guard | VERIFIED | `b9b15dc` pushed to `origin/main` | `server/directRoute.test.ts` 2 tests, full suite baseline 34 files/97 tests, `pnpm check`, production build และ browser direct/selector proof ผ่าน; build มี analytics env/chunk-size warnings เดิม |

### Checkpoint 2026-08-26 — Obsidian-only runtime guard

Browser ตรวจ `?route=game&map=map-002-ashen-obsidian-plains` แล้วเข้าสู่ runtime ของ Obsidian Frontier จริง โดย HUD แสดง Commander Koral / Crashed Leyline Monolith / Glass Stalker และไม่แสดงเนื้อหา map-002. Browser ตรวจ `?route=maps&map=map-002-ashen-obsidian-plains` แล้ว map selector แสดงเพียงการ์ด `01 Obsidian Frontier`; footnote ระบุว่า future maps เป็น planned data และไม่เปิดให้เลือกหรือเตรียม cache. `server/directRoute.test.ts` ผ่าน 2 tests และ `pnpm check` ผ่านหลังเพิ่ม `RUNTIME_MAP_ID`/runtime allow-list และแก้ direct `route=game` transition. หน่วยนี้ผ่าน diff-check, targeted/full validation และ browser proof แล้ว; checkpoint ถูก commit/push เป็น `b9b15dc` บน `origin/main`.

| 2026-08-26 | Obsidian block-first interaction slice | PARTIAL by broad-scope rules; scoped slab/place/persistence behavior verified | `576392e` pushed to `origin/main` | Added coordinate/state/action block modules, correct-vs-wrong tool resolver, 64-cap `structure-001`, solid-support/empty-cell gates, atomic accepted-placement consumption, map+player IndexedDB overrides, offline action validation, four-slot fresh-profile browser proof, and reload proof. `35` test files / `106` tests passed, `pnpm check`, diff-check, and production build passed; existing analytics env and Babylon chunk warnings remain. Broad tree/leaf, gravity, 40-slot carry, and universal block registry remain partial. |
| 2026-08-26 | Obsidian world farming slice | PARTIAL by broad-scope rules; scoped generator/farm/plant/harvest behavior verified | `afe0620` pushed to `origin/main` | Added map-local Obsidian farm plots, soil/biome gate, non-frame elapsed-time growth, seed consumption only after accepted planting, mature-only harvest, map-scoped reward provenance, capped fictional effect/repel rules, runtime crop records, offline farming action validation and React/Babylon bridge. `37` test files / `116` tests passed, `pnpm check`, diff-check and production build passed. Browser verified planting, rehydration, mature elapsed state, keyboard harvest, reward provenance and cleared plot; overlay-intercepted pointer attempt is not counted as harvest proof. Universal ecology, seed-return chain, cactus hazard and full repellent browser proof remain partial. |
| 2026-08-26 | Obsidian chest/storage slice | PARTIAL by broad-scope rules; scoped 27-slot map-local chest transfer verified | `abb3915` pushed to `origin/main` | Added `CARRY_SLOT_LIMIT=40` transfer gate, `CHEST_SLOT_LIMIT=27`, atomic deposit/withdraw with unchanged item instance/provenance, legacy normalization, `[mapId+playerId]` IndexedDB storage, deterministic Obsidian chest mesh/open bridge, responsive ChestSheet, and bounded server sync validators. `39` test files / `127` tests passed, `pnpm check`, diff-check and production build passed. Browser verified open, deposit to `1/27` with carry `8/40`, withdraw back to `0/27` and `9/40`, and deposit/reload/reopen persistence with `Aether Pickaxe` remaining in chest slot 1. Full 40-slot carry normalization, stack merge/overflow across every reward/action, multiple chest/map runtime isolation, and universal world-storage integration remain partial. |
| 2026-08-26 | Obsidian camera/settings slice | PARTIAL by broad-scope rules; scoped three-camera selection, split settings, and map+player persistence verified | `e186223` code + `4c71961` docs pushed to `origin/main` | Added `cameraModes.ts` with first-person/overhead/side, view-distance options 5–50 in steps of 5, and target FPS options 5/15/30/45/60/120; split Global and In-map settings UI; wired UniversalCamera + ArcRotate camera through dynamic `arcane-control` events; hid hero mesh in first-person; added backward-compatible `OfflineMapState.inMapSettings` and persistence. `40` test files / `130` tests passed, `pnpm check`, `git diff --check`, production build passed. Browser verified future-map direct URL fallback, all three camera views, 50-block selection, and reload hydration of overhead/50/60. Target FPS remains UI/persistence-only and view distance maps to three legacy streaming presets; mobile/device and pause/focus evidence remain outstanding. |
| 2026-08-27 | Explicit runtime performance profiles | PARTIAL by broad-scope rules; manual low/balanced/high budget wiring verified | `cdc2766` pushed to `origin/main` | Added persisted `low`/`balanced`/`high` profile policy, effective render-loop FPS ceilings, scene budget metadata, distance-based plant/enemy updates and sleep/wake clamps while preserving Obsidian-only 500m runtime. Focused `4` files / `18` tests, full `65` files / `254` tests, `pnpm check`, `git diff --check`, production build, and browser evidence passed. Browser confirmed Thai profile selection, low `15` view/`24` mob/`24` animation/`16` physics with effective FPS ceiling `30`, balanced `20`/`40`/`48`/`32` with effective `60`, canvas render, and no creator controls. This is not real-device FPS/memory/thermal evidence and does not claim WebGL detection, adaptive auto-tiering, full LOD/culling/pooling or profiler completion. |
| 2026-08-27 | Structured runtime telemetry sampler | PARTIAL by broad-scope rules; bounded QA-only cadence/mesh snapshot verified | `9c67ded` pushed to `origin/main` | Added `runtimePerformanceTelemetry.ts` bounded in-memory sampler with rendered/throttled callback counts, average/p95/worst frame cadence, sample-window reset, finite-value normalization and capped interval buffer; wired `GameCanvas` to emit at most one snapshot per window and `ArcaneFrontier` to expose QA-only `data-telemetry-*` attributes without player profiler UI or network persistence. Focused `5` files / `23` tests, full `66` files / `259` tests, `pnpm check`, `git diff --check`, production build, and browser smoke passed. Browser observed balanced `60` ceiling, `1342` total meshes, `610–630` active meshes and approximately `7` rendered frames per one-second window in sandbox; this is a local observation, not device acceptance. |
| 2026-08-27 | Tier-aware spatial visibility optimization | PARTIAL by broad-scope rules; Obsidian world/farm/storage mesh activation policy verified | `463bd63` pushed to `origin/main` | Added `runtimeVisibilitySystem.ts` pure distance/broken-state policy and wired Obsidian module/dynamic/farm/storage meshes to the active performance view budget while preserving canonical world/action state and future-map guards. Focused `6` files / `27` tests, full `67` files / `263` tests, `pnpm check`, `git diff --check`, production build, and browser smoke passed. In a separate sandbox browser run active meshes were `418` versus prior telemetry runs `610–630`, rendered frames `9` versus `7`, but cadence remained below effective 60 FPS; comparison is directional/non-controlled and not device acceptance. |
| 2026-08-27 | Admin-only runtime profiler preview | PARTIAL by broad-scope rules; snapshot analyzer and Thai Workbench boundary verified | `46dff1f` pushed to `origin/main` | Added pure `runtimePerformanceProfiler.ts`, bounded creator snapshot schema/route under `adminProcedure`, and a Thai Workbench profiler panel that reports observed FPS, frame budget, active-mesh ratio and policy recommendations without player/runtime mutation or network persistence. Focused profiler tests passed; full `68` files / `267` tests, `pnpm check`, `git diff --check`, production build passed. Browser confirmed player `/` has no profiler panel/text and unauthenticated `/creator-workbench` is gated before the form. No admin-authenticated mutation proof, device benchmark, GPU timing, durable history or auto-adaptive tier claim. |
| 2026-08-27 | Generic creator domain artifact registry | PARTIAL by broad-scope rules; additive metadata schema/service and no-code boundary verified | `dab4d3e` pushed to `origin/main` | Added additive `creatorDomainArtifacts` table/migration, deterministic canonical metadata hash/key, provenance/runtime policy checks, binary payload rejection, metadata-only admin routes `creator.artifact.preview/register/list` and a structured Thai Workbench registry form/list. Focused registry tests passed; full `69` files / `270` tests, `pnpm check`, `git diff --check`, production build passed. Browser confirmed player route has no registry/profiler text and unauthenticated creator route is gated. No DB migration apply, admin-authenticated insert/list, object-storage upload, domain-specific asset bytes, runtime publish or rollback/delete claim. |
| 2026-08-27 | Generic artifact review workflow | PARTIAL by broad-scope rules; transition policy and admin-only controls verified | `e7f41f5` pushed to `origin/main` | Added additive review fields/index/migration `0005_careful_artifact_review.sql`, pure `draft → approved/rejected → draft` transition policy with required notes for reject/reopen, optimistic status guard in `creator.artifact.review`, list review-status filter and Thai Workbench review controls. Focused review/registry tests passed; full `70` files / `273` tests, `pnpm check`, `git diff --check`, production build passed. Browser confirmed player route has no review/registry/profiler controls and unauthenticated creator route is gated before review controls. No admin-authenticated DB mutation, migration apply, durable audit report, delete/rollback override or runtime publish claim. |
| 2026-08-27 | Immutable creator artifact review audit trail | PARTIAL by broad-scope rules; transactional event contract and admin-only history view verified | `d2b1c25` pushed to `origin/main` | Added additive migration `0006_immutable_artifact_review_events.sql`, immutable review-event table, transactional status+event write path, admin `creator.artifact.audit` route and Thai Workbench history view. Focused review/registry tests passed; full `70` files / `273` tests, `pnpm check`, `git diff --check`, production build passed. Browser confirmed player route has no review/registry/profiler controls and unauthenticated creator route is gated. No production DB apply or admin-authenticated audit E2E claim. |
| 2026-08-27 | Approved-only creator artifact metadata export preview | PARTIAL by broad-scope rules; pure approved gate and admin route/UI boundary verified | `eb93da9` pushed to `origin/main` | Added `creatorDomainArtifactExport.ts`, approved-only `creator.artifact.export` route and Workbench export preview action. Export returns metadata only with `assets: []`, `publishReady: false`, and runtime import/player visibility/cache disabled; draft/rejected/missing audit/runtime-policy records are rejected. Focused export tests passed; full `71` files / `276` tests, `pnpm check`, `git diff --check`, production build passed. Browser confirmed player route has no export/review/registry/profiler controls and unauthenticated creator route is gated. No admin-authenticated DB export E2E, downloadable bytes, object-storage upload, asset compatibility validator or runtime publish claim. |
| 2026-08-27 | Strict creator artifact runtime compatibility preview | PARTIAL by broad-scope rules; pure allow-list/provenance validator and admin/player boundary verified | `bcbb800` pushed to `origin/main` | Added `creatorDomainArtifactCompatibility.ts`, strict reason-coded validator, admin `creator.artifact.compatibility` route and Thai Workbench target-map preview. Only `obsidian-frontier` is reviewable; future targets are blocked, and output always keeps `publishReady: false`/`runtimeImportAllowed: false`. Focused compatibility/auth tests passed; full `72` files / `279` tests, `pnpm check`, `git diff --check`, production build passed. Browser confirmed player route has no compatibility/registry/review/export/profiler controls and unauthenticated creator route is gated before target-map selector/action. No admin-authenticated DB validation E2E, binary/model compatibility, runtime import, map unlock or publish claim. |
| 2026-08-27 | No-code pixel composition manifest preview | PARTIAL by broad-scope rules; pure builder, admin route and creator/player boundary verified | `a61d5a7` pushed to `origin/main` | Added `creatorCompositionBuilder.ts`, schema-validated `creator.composition.preview`, Thai Workbench composition card with template/subject/canvas/color/part controls, deterministic registry-compatible metadata hash, pixel budget and explicit `meshRequired: false`. Builder rejects out-of-bounds parts, unknown layers, duplicate ids and binary/geometry keys. Focused composition/auth tests passed; full `73` files / `282` tests, `pnpm check`, `git diff --check`, production build passed. Browser confirmed player route has no composition/layer/palette text and unauthenticated creator route is gated before composition editor. No pixel-by-pixel canvas, actual texture/model/skeleton bytes, DB registration, asset export or runtime publish claim. |
| 2026-08-27 | Bounded no-code pixel-cell editor | PARTIAL by broad-scope rules; sparse pixel-cell validator, Thai grid UI and creator/player code boundary verified | `f286663` pushed to `origin/main` | Added bounded sparse `pixels[]` contract with palette references, bounds/duplicate/unknown-color validation and deterministic y/x ordering before registry hash; added `CreatorPixelGridEditor` with Thai palette selection, click-to-paint/toggle and clear controls, plus painted-pixel summary. Focused composition/registry/router tests passed; full `73` files / `282` tests, `pnpm check`, `git diff --check`, and production build passed. Browser evidence in `docs/pixel-cell-player-browser-evidence-2026-08-27.md` confirmed player route has no composition/pixel controls and unauthenticated creator route renders only the developer gate; no authenticated admin preview claim. UI supports 32×32 display while validator/schema accepts up to 128×128 and 16,384 cells. No texture atlas/model/skeleton bytes, DB registration, asset export or runtime publish claim. |
| 2026-08-27 | Composition-to-texture handoff preview | PARTIAL by broad-scope rules; pure sparse-to-RGBA adapter, existing texture.pack builder reuse, provenance/hash binding and admin auth verified | `dd4f744` pushed to `origin/main` | Added `creatorCompositionTextureAdapter.ts` and `creator.composition.texturePreview`. Sparse palette cells become a transient RGBA layer through the existing `texture.pack` builder; provenance includes the composition SHA-256, output validation/hash is reused, and response explicitly requires separate registration/review while keeping runtime import/player visibility/cache disabled. Focused adapter/router tests passed (`14` adapter/composition/router tests); full `74` files / `287` tests, `pnpm check`, `git diff --check`, and production build passed. Browser boundary evidence in `docs/pixel-cell-player-browser-evidence-2026-08-27.md` remains player/unauthenticated-gate only; no authenticated admin preview claim. No automatic registry write, storage upload, downloadable export, binary/model compatibility or runtime publish claim. |
| 2026-08-27 | Layer-aware composition pixels | PARTIAL by broad-scope rules; layerId validation, per-layer overlap, Thai layer selector and z-index/opacity flattening verified | `a180ca9` pushed to `origin/main` | Every sparse pixel now records `layerId`; duplicate coordinates are rejected only within the same layer, while the same coordinate can be painted across distinct layers. `CreatorPixelGridEditor` switches the visible editing layer, and the adapter composites visible cells by z-index and opacity before passing one RGBA layer to the existing builder. Focused builder/adapter/router tests passed (`16` tests); full `74` files / `289` tests, `pnpm check`, `git diff --check`, and production build passed. Fresh browser evidence in `docs/pixel-cell-player-browser-evidence-2026-08-27.md` confirmed player route remains free of layer/pixel/texture creator controls; creator access remained an unauthenticated developer gate, so no admin preview claim is made. No texture atlas/model/skeleton bytes, DB registration, binary compatibility or runtime publish claim. |
| 2026-08-27 | Shared creator template catalog | PARTIAL by broad-scope rules; CreatorStudio and Workbench use one template/skin-part source-of-truth | `eb4b9ed` pushed to `origin/main` | Extracted `client/src/lib/creatorTemplateCatalog.ts` from CreatorStudio’s local definitions. The catalog provides stable template IDs, categories, kinds, dimensions, the bounded `survivor-pixel-32` preset and skin layout parts; CreatorStudio now aliases the shared catalog, while Workbench filters unsupported atlas templates and uses catalog-backed selection/metadata. Focused catalog/composition/router tests passed (`19` tests); full `75` files / `292` tests, `pnpm check`, `git diff --check`, and production build passed. Fresh browser evidence in `docs/pixel-cell-player-browser-evidence-2026-08-27.md` confirmed the player route remains free of catalog/template/skin/layer/pixel/texture creator controls; creator authentication was not available, so no authenticated creator preview claim is made. No texture atlas export, model/skeleton bytes, DB registration or runtime publish claim. |
| 2026-08-27 | Template-driven composition selection | PARTIAL by broad-scope rules; template kind mapping, 32×32 Workbench filtering and safe draft reset verified | `44e4ce3` pushed to `origin/main` | Added pure `getCompositionSubjectForTemplate`/`isWorkbenchCompositionTemplate` policy. Workbench now selects supported presets from the shared catalog, maps tile/skin/weapon/icon kinds to composition subjects, applies preset dimensions to canvas and body part, and clears sparse pixel draft when changing template to prevent stale coordinates crossing assets. Focused catalog/composition/router tests passed (`20` tests); full `75` files / `293` tests, `pnpm check`, `git diff --check`, and production build passed. Fresh player browser evidence in `docs/pixel-cell-player-browser-evidence-2026-08-27.md` shows no creator controls; unauthenticated creator preview was not claimed. No texture atlas export, model/skeleton bytes, DB registration or runtime publish claim. |
| 2026-08-27 | Composition texture export preview | PARTIAL by broad-scope rules; validated PNG bundle, deterministic export identity, explicit download action and admin auth verified | `4a63832` pushed to `origin/main` | Added pure `creatorCompositionTextureExport.ts`, admin `creator.composition.exportPreview` route and Thai Workbench export result with explicit `ดาวน์โหลด PNG preview` action. The export revalidates PNG bytes, asset SHA and manifest hash, binds export identity to composition SHA/pack SHA/asset hashes, returns deterministic `downloadFileName` and keeps `runtimeImportAllowed`, `playerVisible`, `cacheable` disabled; registration and review remain separate. Focused export/adapter/router tests passed (`19` tests); full `76` files / `296` tests, `pnpm check`, `git diff --check`, and production build passed. Fresh browser evidence in `docs/pixel-cell-player-browser-evidence-2026-08-27.md` confirms player route has no export/download or other creator controls; sandbox creator access remained unauthenticated, so no admin export execution or download claim is made. No automatic browser download, object-storage upload, DB registration, atlas/model/skeleton bytes or runtime publish claim. |
| 2026-08-27 | Composition export manifest sidecar | PARTIAL by broad-scope rules; deterministic UTF-8 `manifest.json`, manifest SHA, explicit sidecar download action and runtime boundary verified | `d559bd8` pushed to `origin/main` | Extended `creatorCompositionTextureExport.ts` to include a newline-terminated stable-JSON `manifest.json` sidecar, base64 bytes, MIME, SHA-256 and an export identity bound to composition/pack/manifest/asset hashes. Workbench now exposes separate PNG and manifest download buttons, both manual actions; route remains admin-only and runtime import/player visibility/cache remain disabled. Focused export/adapter/router tests passed (`20` tests); full `76` files / `297` tests, `pnpm check`, `git diff --check`, and production build passed. Fresh browser evidence in `docs/pixel-cell-player-browser-evidence-2026-08-27.md` confirms no manifest/export/download controls in player; no authenticated creator execution or download claim is made. No zip/package assembly, object-storage upload, DB registration or runtime publish claim. |
| 2026-08-27 | Deterministic composition ZIP texture bundle | PARTIAL by broad-scope rules; deterministic ZIP file ordering/signature/path validation, bundle SHA and manual Workbench download action verified | `2980907` pushed to `origin/main` | Added `creatorCompositionTextureBundle.ts` with fixed ZIP metadata, sorted `manifest.json` + relative PNG paths, CRC-32 and bounded path checks; `creatorCompositionTextureExport.ts` now returns ZIP bytes/base64, file list and bundle SHA tied into export identity. Workbench exposes `ดาวน์โหลด ZIP texture pack` as a manual action. Focused bundle/export/adapter/router tests passed (`22` tests); full `77` files / `299` tests, `pnpm check`, `git diff --check`, and production build passed. Fresh player browser evidence in `docs/pixel-cell-player-browser-evidence-2026-08-27.md` confirms no ZIP/manifest/export/download controls in player; no authenticated creator package download claim is made. No durable storage, DB registration, multi-asset domain package policy, binary/model compatibility or runtime publish claim. |
| 2026-08-27 | Creator texture byte compatibility preview | PARTIAL by broad-scope rules; admin-only reason-coded verification of manifest/PNG/ZIP hashes, CRC, decompression and file lists verified | `0ea20d8` pushed to `origin/main` | Added pure `creatorCompositionTextureCompatibility.ts` and admin `creator.composition.byteCompatibility` route. The validator rechecks export flags, lowercase SHA-256 format, manifest JSON/content/hash, PNG signatures/asset hashes, ZIP local/central records, CRC/decompression, bundle hash and exact file list/byte parity; Workbench exposes Thai `ตรวจ PNG/manifest/ZIP bytes` action and reports `compatible`/`blocked` without import/publish. Focused compatibility/bundle/export/adapter/router tests passed (`26` tests); full `78` files / `303` tests, `pnpm check`, `git diff --check`, and production build passed. Fresh player browser evidence in `docs/pixel-cell-player-browser-evidence-2026-08-27.md` confirms no compatibility/export/download controls in player; no authenticated creator validation claim is made. No durable storage, DB registration, model/geometry compatibility or runtime publish claim. |
| 2026-08-27 | Explicit composition texture registration preflight | PARTIAL by broad-scope rules; verified composition→texture build and byte compatibility preflight, explicit admin register action and durable owner reuse verified; DB/storage execution intentionally not claimed | `c9c4cfe` pushed to `origin/main` | Added pure `creatorCompositionTextureRegistration.ts` and admin `creator.composition.register`. The helper rebuilds the composition, validates existing `texture.pack` output, creates PNG/manifest/ZIP export, runs byte compatibility, and only then calls existing `registerTexturePackArtifact`; the route is a separate manual action, keeps runtime import false and does not publish automatically. Focused registration/compatibility/bundle/export/adapter/router tests passed (`26` tests); full `79` files / `305` tests, `pnpm check`, `git diff --check`, and production build passed. Fresh player browser evidence in `docs/pixel-cell-player-browser-evidence-2026-08-27.md` confirms no register/registry/download/creator controls in player. No DATABASE_URL/object-storage E2E, migration/db push, DB row, upload, review approval, ZIP persistence or runtime publish claim. |
| 2026-08-27 | Animation composition to skin texture contract | PARTIAL by broad-scope rules; animation→skin kind, parts-derived `skinLayout`, `skins/*.png` manifest path and provenance validation verified | `035f474` pushed to `origin/main` | Extended `creatorCompositionTextureAdapter.ts` so animation subjects become `skin` assets and derive `composition-parts-v1` layout from validated composition parts; existing texture builder then enforces skin layout geometry and emits `skins/<assetId>.png`. Focused adapter/export/router tests passed (`20` tests); full `76` files / `297` tests, `pnpm check`, `git diff --check`, and production build passed. Fresh browser evidence in `docs/pixel-cell-player-browser-evidence-2026-08-27.md` confirms no skin/texture/export creator controls in player; no authenticated creator preview or skin download claim is made. No skeleton/rig/model bytes, object-storage upload, DB registration, binary compatibility or runtime publish claim. |
| 2026-08-27 | Texture artifact review workflow | PARTIAL by broad-scope rules; admin-only texture status transitions, filtered list, immutable review-event owner and explicit Thai Workbench controls implemented; DB/storage execution intentionally not claimed | `1d40925` pushed to `origin/main` | Added `reviewStatus`/review metadata to `creatorArtifacts`, additive migration `0007_texture_artifact_review.sql`, separate `creatorArtifactReviewEvents`, and `reviewTexturePackArtifact`/list/audit owner methods that reuse the existing transition policy. `creator.texture.review`, `creator.texture.list` and `creator.texture.audit` remain admin-only; Workbench exposes manual approve/reject/reopen controls and clearly keeps approved artifacts out of runtime import/publish/cache. Full `79` files / `307` tests, `pnpm check`, and production build passed; fresh player browser boundary evidence confirms no review/list/audit/register/creator controls in player and unauthenticated `/creator-workbench` remains gated. No migration/db push, DB row, object-storage review E2E, authenticated admin review E2E or runtime publish claim. |
| 2026-08-27 | Bounded multi-part composition selector | PARTIAL by broad-scope rules; canonical 32×32 non-overlapping body-part presets, minimum-one selection, animation→skin parts manifest and existing skin-layout handoff verified; DB/storage/runtime execution intentionally not claimed | `8307b45` pushed to `origin/main` | Added `CREATOR_WORKBENCH_COMPOSITION_PARTS` and Workbench selector for head/face/torso/arms/legs. Selected presets compile to deterministic `parts[]` with slot, bounds and layer mapping for animation skin; non-animation subjects retain the existing custom `body` part path. Catalog and builder tests verify all seven parts fit within 32×32 without overlap, no unsafe binary/geometry fields are introduced, multi-part manifests remain deterministic, and full `79` files / `309` tests plus `pnpm check`, `git diff --check`, and production build passed. Fresh player and unauthenticated Workbench browser evidence confirms no multi-part/creator controls are exposed. No per-part paint viewport/lock, pixel-to-part assignment, atlas/model/skeleton bytes, DB/storage E2E, authenticated creator E2E or runtime publish claim. |
| 2026-08-27 | Dependency graph core validation and inspector | PARTIAL by broad-scope rules; deterministic node/edge normalization, required/optional reference checks, kind/generator/version/hash compatibility checks, cycle detection, topological order, seed/rules metadata and admin-only Workbench preview verified | `2444bc7` pushed to `origin/main` | Added `server/generators/dependencyGraph.ts` with `a-survival.dependency-graph.v1` and the initial `creator.dependencyGraph.preview` contract, plus Thai Workbench controls for seed/rules version. Pure tests cover valid ordering and all blocking reason families; router tests cover admin success and user/unauthenticated `FORBIDDEN`. Final full suite at that checkpoint passed `80` files / `314` tests, `pnpm check`, `git diff --check`, and production build; fresh player and unauthenticated Workbench evidence confirms no graph/generator controls in player. No common orchestrator, automatic dependency emission from all generators, DB/storage write, authenticated creator E2E, binary asset validation or runtime publish/import claim. |
| 2026-08-27 | Actual content-catalog dependency graph integration | PARTIAL by broad-scope rules; registered `content.catalog` artifact generation, category asset/definition node derivation, deterministic hash/reference validation, bounded sampling and admin-only Workbench preview verified | `d3da3ac` pushed to `origin/main` | Added `server/generators/contentCatalogDependencyGraph.ts` and `creator.dependencyGraph.contentCatalogPreview`. The adapter generates the existing `content.catalog` artifact from the submitted seed through `CommonGeneratorRegistry`, derives category asset nodes and sampled definition nodes, and validates their references through `a-survival.dependency-graph.v1`; Workbench now calls this actual adapter rather than a static World→Biome demo. Integration/pure/router tests passed and final run passed `81` files / `317` tests, `pnpm check`, `git diff --check`, and production build; fresh player and unauthenticated Workbench evidence confirms no graph/generator controls in player. No World/Biome/Structure/Quest cross-generator orchestrator, automatic dependency emission from every generator, DB/storage write, authenticated creator E2E, binary asset validation or runtime publish/import claim. |
| 2026-08-27 | Quest-to-content-catalog dependency integration | PARTIAL by broad-scope rules; registered `quest.progression` artifact generation, playable/planned map nodes, sampled quest prerequisite/objective/reward references, catalog linkage, unresolved-reference blocking and admin-only Workbench preview verified | `be1f969` + `3fc5934` pushed to `origin/main` | Added `server/generators/questContentCatalogDependencyGraph.ts` and `creator.dependencyGraph.questContentCatalogPreview`. The adapter generates real quest progression plus the existing catalog (with the required catalog-node exposure fix in `3fc5934`), includes planned map metadata without runtime import/cache/player visibility, and routes objective/reward references through the dependency validator; existing generator IDs such as `terrain.obsidian` or `npc-map-warder` remain visible as blocking missing references instead of being silently invented. Tests cover deterministic output, future-map count, unresolved reasons, bounds and admin/user/unauthenticated routing; final run passed `82` files / `320` tests, `pnpm check`, `git diff --check` and production build. Fresh player and unauthenticated Workbench browser evidence confirms no quest graph/generator controls are exposed in player and the Workbench remains gated. No DB/storage write, authenticated creator E2E, binary asset validation or runtime publish/import claim. |

| 2026-08-27 | World-to-structure dependency integration | PARTIAL by broad-scope rules; real Obsidian world generation, structure blueprint/placement generation, deterministic world/structure/placement hashes, bounded placement candidates, rejection summary and admin-only Workbench preview verified | `15e0d27` pushed to `origin/main` | Added `server/generators/worldStructureDependencyGraph.ts` and `creator.dependencyGraph.worldStructurePreview`. The adapter reuses `tools/world-generator.ts` for `obsidian-frontier`, maps terrain cells into structure placement contexts, invokes the existing `structure.placement` registry and validates world→blueprint→run→placement dependency nodes. The Workbench adds a manual World → structure source with radius control; planned/future map count remains zero and graph runtime policy stays disabled. Focused route/adapter tests passed `83` files / `323` tests at the current test run, `pnpm check` and `git diff --check`; fresh player and unauthenticated Workbench browser evidence confirms no world/structure graph controls are exposed. No authenticated preview, DB/storage write, asset/model bytes, full structure runtime placement or player publish/import claim. |

| 2026-08-27 | Item-to-content-catalog dependency integration | PARTIAL by broad-scope rules; real universal item artifact, balance budget, sampled catalog category/resource reference checks, unresolved required-edge blocking and admin-only Workbench preview verified | `aee384d` pushed to `origin/main` | Added `server/generators/itemContentCatalogDependencyGraph.ts` and `creator.dependencyGraph.itemContentCatalogPreview`. The adapter reuses `item.universal` and `content.catalog`, reports balance score, keeps category/resource references explicit and converts missing references into required graph dependencies so validation blocks before registry. Workbench adds a manual Item → content reference source and item ID field; graph runtime policy remains disabled. Final validation passed `84` files / `326` tests, `pnpm check`, `git diff --check`, and production build (only known analytics-placeholder and Babylon vendor chunk warnings). Fresh `/` browser evidence shows player-only controls; unauthenticated `/creator-workbench` shows only the Thai `DEVELOPER ONLY` gate. No authenticated preview, DB/storage write, binary asset generation, runtime publish/import or player UI claim; boundary details are in `docs/item-content-catalog-browser-evidence-2026-08-27.md`. |
| 2026-08-27 | World-to-block-to-content-catalog dependency integration | PARTIAL by broad-scope rules; real `world.generator` block/resource output, runtime block definition/drop/block-item/asset references, bounded sample graph, required missing-reference blocking and admin-only Workbench preview verified | `176a6cf` pushed to `origin/main` | Added `server/generators/worldBlockContentCatalogDependencyGraph.ts` and `creator.dependencyGraph.worldBlockContentCatalogPreview`. The adapter reuses `generateWorld` for the playable-map owner, `blockModules` for canonical block semantics, and `content.catalog` for sampled content nodes; missing runtime block-item or block-asset mappings remain explicit required graph errors rather than invented definitions. Workbench adds a manual World → block → content catalog source with sample bound and shows unresolved reason cards. Final validation passed `85` files / `329` tests, `pnpm check`, `git diff --check`, and production build (only known analytics-placeholder and Babylon vendor chunk warnings). Browser evidence confirms player-only `/` and the unauthenticated `DEVELOPER ONLY` Workbench gate in `docs/world-block-content-catalog-browser-evidence-2026-08-27.md`. No authenticated graph mutation, database/storage write, binary asset generation, runtime publish/import/cache, or device/mobile acceptance claim. |
| 2026-08-27 | Structure-to-block-to-content-catalog dependency integration | PARTIAL by broad-scope rules; real structure/world/block/catalog owners, selected blueprint and child relations, bounded structure-block sample, required missing asset/drop/block-item blockers, and admin-only Workbench preview verified | `b34aa00` pushed to `origin/main` | Added `server/generators/structureBlockContentCatalogDependencyGraph.ts` and `creator.dependencyGraph.structureBlockContentCatalogPreview`. The adapter reuses the real world/structure dependency graph, `world.generator` structure block groups, `structureGenerator` blueprints, `blockModules`, and `content.catalog`; missing blueprint assets, required children, block assets, drops, or runtime block items are explicit required graph errors. Workbench adds a manual Structure → block → content catalog source and bounded sample controls. Final validation passed `86` files / `332` tests, `pnpm check`, `git diff --check`, and production build (only known analytics-placeholder and Babylon vendor chunk warnings). Browser evidence confirms player-only `/` and the unauthenticated `DEVELOPER ONLY` Workbench gate in `docs/structure-block-content-catalog-browser-evidence-2026-08-27.md`. No authenticated graph mutation, database/storage write, binary asset generation, runtime publish/import/cache, or device/mobile acceptance claim. |
| 2026-08-27 | World-to-biome/resource-to-content-catalog dependency integration | PARTIAL by broad-scope rules; real Obsidian biome cells and resource nodes, deterministic biome/resource graph nodes, bounded resource sampling, required missing biome/resource-definition blockers, and admin-only Workbench preview verified | `a1a27c2` pushed to `origin/main` | Added `server/generators/worldBiomeResourceContentCatalogDependencyGraph.ts` and `creator.dependencyGraph.worldBiomeResourceContentCatalogPreview`. The adapter reuses `world.generator` terrain/resource output and `content.catalog`; `content.catalog` currently has no biome category or matching `ore.aether.block` definition, so these references remain explicit required graph errors rather than fabricated content. Workbench adds a manual World → biome/resource → content catalog source and bounded resource sample control. Final validation passed `87` files / `335` tests, `pnpm check`, `git diff --check`, and production build (only known analytics-placeholder and Babylon vendor chunk warnings). Browser evidence confirms player-only `/` and the unauthenticated `DEVELOPER ONLY` Workbench gate in `docs/world-biome-resource-content-catalog-browser-evidence-2026-08-27.md`. No authenticated graph mutation, database/storage write, binary asset generation, runtime publish/import/cache, or device/mobile acceptance claim. |
| 2026-08-27 | World spawn dependency integration | PARTIAL by broad-scope rules; real `GeneratedSpawnPoint` output, biome and structure context edges, bounded spawn sampling, explicit missing mob/NPC species definitions, and admin-only Workbench preview verified | `622dcad` pushed to `origin/main` | Added `server/generators/worldSpawnDependencyGraph.ts` and `creator.dependencyGraph.worldSpawnPreview`. The adapter reuses `world.generator` on `obsidian-frontier`, samples spawn points deterministically, links real biome and `structureId` context, and keeps missing species definitions as required graph blockers rather than inventing an ecology generator. Workbench adds a manual World → spawn context source and sample control. Final validation passed `88` files / `338` tests, `pnpm check`, `git diff --check`, and production build (only known analytics-placeholder and Babylon vendor chunk warnings). Browser evidence confirms player-only `/` and the unauthenticated `DEVELOPER ONLY` Workbench gate in `docs/world-spawn-browser-evidence-2026-08-27.md`. No authenticated graph mutation, database/storage write, binary asset generation, runtime publish/import/cache, or device-mobile acceptance claim. |
| 2026-08-27 | World spawn-to-procedural-loot dependency integration | PARTIAL by broad-scope rules; real world spawn output, real `content.generator` loot/drop artifacts, bounded regular/boss sampling, explicit species and asset-manifest blockers, and admin-only Workbench preview verified | `2a00331` pushed to `origin/main` | Added `server/generators/worldSpawnLootDependencyGraph.ts` and `creator.dependencyGraph.worldSpawnLootPreview`. The adapter reuses `world.generator` spawn points and `content.generator`/`generateLootDrop`, creates deterministic loot and drop-item nodes for sampled regular/boss spawns, links them to biome/structure context, and keeps missing mob/NPC species definitions plus unverified texture asset bindings as required blockers. Workbench adds a manual World → spawn → loot source and sample control. Final validation passed `89` files / `341` tests, `pnpm check`, `git diff --check`, and production build (only known analytics-placeholder and Babylon vendor chunk warnings). Browser evidence confirms player-only `/` and the unauthenticated `DEVELOPER ONLY` Workbench gate in `docs/world-spawn-loot-browser-evidence-2026-08-27.md`. No authenticated graph mutation, database/storage write, asset-manifest binding, runtime publish/import/cache, or device/mobile acceptance claim. |
| 2026-08-27 | Procedural content-to-content-catalog dependency integration | PARTIAL by broad-scope rules; real `content.generator` procedural weapons, bounded category/count preview, catalog category asset linkage, required exact-definition/magic-category/texture-asset blockers, and admin-only Workbench preview verified | `88f0b3f` pushed to `origin/main` | Added `server/generators/proceduralContentCatalogDependencyGraph.ts` and `creator.dependencyGraph.proceduralContentCatalogPreview`. The adapter reuses `generateProceduralWeapons` and the registered `content.catalog`; generated weapons connect to matching logical category assets when present, while exact generated IDs, the absent magic catalog category, and unverified asset bindings remain required graph errors. Workbench adds a manual Procedural content → content catalog source with category/count controls. Final validation passed `90` files / `345` tests, `pnpm check`, `git diff --check`, and production build (only known analytics-placeholder and Babylon vendor chunk warnings). Browser evidence confirms player-only `/` and the unauthenticated `DEVELOPER ONLY` Workbench gate in `docs/procedural-content-catalog-browser-evidence-2026-08-27.md`. No authenticated graph mutation, database/storage write, asset-manifest binding, runtime publish/import/cache, or device/mobile acceptance claim. |
| 2026-08-27 | Plant-to-content-catalog dependency integration | PARTIAL by broad-scope rules; real 300-entry runtime plant catalog, exact seed/harvest/soil/biome links, bounded plant sampling, category asset reuse, required missing-reference blocking, and admin-only Workbench preview verified | `5dfa9b2` pushed to `origin/main` | Added `server/generators/plantContentCatalogDependencyGraph.ts` and `creator.dependencyGraph.plantContentCatalogPreview`. The adapter reuses `generateWorldPlantCatalog` and `getPlantDefinition`, keeps `obsidian-frontier` context, links existing `content.catalog` plant/seed category assets, and leaves exact seed/harvest definitions, soil/biome nodes, runtime plant assets, and seed icon bindings as required missing edges rather than inventing catalog definitions or assets. Workbench adds a manual Plant → content catalog source with a bounded sample input and unresolved reason cards. Final validation passed `91` files / `348` tests, `pnpm check`, `git diff --check`, and production build (only known analytics-placeholder and Babylon vendor chunk warnings). Browser evidence in `docs/plant-content-catalog-browser-evidence-2026-08-27.md` confirms player-only `/` and the unauthenticated `DEVELOPER ONLY` Workbench gate. No authenticated graph E2E, database/storage write, asset-manifest binding, runtime publish/import/cache, or device/mobile acceptance claim. |
| 2026-08-27 | Procedural content-to-Universal Item Engine dependency integration | PARTIAL by broad-scope rules; real `content.generator` weapons, real `item.universal` registry/plugin conversion, bounded category/count/budget preview, deterministic balance summaries, and required asset/validation blockers verified | `154425b` pushed to `origin/main` | Added `server/generators/proceduralUniversalItemDependencyGraph.ts` and `creator.dependencyGraph.proceduralUniversalItemPreview`. The adapter maps procedural category/element/rarity into canonical Universal Item fields and invokes the registered Universal Item Engine, while missing procedural asset-manifest bindings and any Universal Item validation failures remain required graph errors. Workbench adds a manual Procedural content → item.universal source with count/budget controls and blocker reason cards. Final validation passed `92` files / `352` tests, `pnpm check`, `git diff --check`, and production build (only known analytics-placeholder and Babylon vendor chunk warnings). Browser evidence in `docs/procedural-universal-item-browser-evidence-2026-08-27.md` confirms player-only `/` and the unauthenticated `DEVELOPER ONLY` Workbench gate. No authenticated graph E2E, database/storage write, asset-manifest binding, registry write, runtime publish/import/cache, or device/mobile acceptance claim. |
| 2026-08-27 | Animation profile-to-active asset metadata dependency integration | PARTIAL by broad-scope rules; real `animation.profile` output, active runtime `arcane-frontier-voxel-pixel` manifest, real `metadata/animations.json` state comparison, exact asset-entry/kind checking, required missing/mismatch blockers, and admin-only Workbench preview verified | `c3349da` pushed to `origin/main` | Added `server/generators/animationAssetDependencyGraph.ts` and `creator.dependencyGraph.animationAssetPreview`. The adapter invokes the registered animation profile owner, reads the active pack manifest and animation metadata as read-only repository inputs, connects pack → metadata entry → runtime metadata → profile, and leaves the default `animation.survivor.default` binding blocked because the active manifest has no exact animation entry. Existing `data.animations` kind mismatch and profile-state drift remain explicit validator blockers; no metadata rewrite or clip/asset fabrication occurs. Workbench adds `Animation profile → asset metadata` source and blocker card, reusing the Thai animation fields. Final validation passed `93` files / `357` tests, `pnpm check`, `git diff --check`, full `pnpm test -- --run`, and production build (only known analytics-placeholder and Babylon vendor chunk warnings). Browser evidence in `docs/animation-asset-browser-evidence-2026-08-27.md` confirms player-only `/` and unauthenticated `DEVELOPER ONLY` Workbench gate. No authenticated graph E2E, database/storage write, asset generation, runtime animation-profile consumption, registry write, runtime publish/import/cache, or device/mobile acceptance claim. |
| 2026-08-27 | Structure-to-spawn reconciliation dependency integration | PARTIAL by broad-scope rules; real `world.generator` world/spawn output, real `structure.placement` blueprint/placement output, bounded role/species/proximity reconciliation, explicit spawn-intent/species/context blockers, and admin-only Workbench preview verified | `37eab0a` pushed to `origin/main` | Added `server/generators/structureSpawnReconciliationDependencyGraph.ts` and `creator.dependencyGraph.structureSpawnReconciliationPreview`. The adapter reuses the real structure source export, structure blueprint NPC/mob spawn intent, Obsidian world spawn points, and world structure context. It creates deterministic reconciliation records and central graph nodes; missing expected spawns, species mismatches, and structure-linked spawn context without a matching generated placement remain required blockers. It does not fabricate mob/NPC definitions or connect the still-hardcoded map encounter owners to generated IDs. Workbench adds a manual Structure → spawn reconciliation source and blocker card using bounded radius/sample controls. Final validation passed `94` files / `361` tests, `pnpm check`, `git diff --check`, full `pnpm test -- --run`, and production build (only known analytics-placeholder and Babylon vendor chunk warnings). Browser evidence in `docs/structure-spawn-reconciliation-browser-evidence-2026-08-27.md` confirms player-only `/` and unauthenticated `DEVELOPER ONLY` Workbench gate. No authenticated graph E2E, database/storage write, mob/NPC generation, runtime encounter consumption, registry write, runtime publish/import/cache, or device/mobile acceptance claim. |
| 2026-08-27 | World spawn loot-to-Universal Item dependency integration | PARTIAL by broad-scope rules; real world spawn/biome/structure context, real `content.generator` loot/drop records, canonical `item.universal` conversion, bounded sample/budget preview, explicit asset and Universal Item validation blockers, and admin-only Workbench preview verified | `61d1ec7` pushed to `origin/main` | Added `server/generators/worldSpawnLootUniversalItemDependencyGraph.ts` and `creator.dependencyGraph.worldSpawnLootUniversalItemPreview`; exported the existing deterministic world-loot source and reused the canonical `buildUniversalItemInput` mapping rather than creating a second conversion contract. Each real loot drop is passed through the registered Universal Item Engine, while existing species/context/asset blockers remain required and low-budget validation failures become explicit `item.universal.output` blockers. Workbench adds a manual World spawn loot → item.universal source with bounded spawn-sample and power-budget controls plus blocker reasons. Final validation passed `95` files / `365` tests, `pnpm check`, `git diff --check`, full `pnpm test -- --run`, and production build (only known analytics-placeholder and Babylon vendor chunk warnings). Browser evidence in `docs/world-spawn-loot-universal-item-browser-evidence-2026-08-27.md` confirms player-only `/` and unauthenticated `DEVELOPER ONLY` Workbench gate. No authenticated graph E2E, database/storage write, asset-manifest binding, mob/NPC definition, runtime loot/item consumption, registry write, runtime publish/import/cache, or device/mobile acceptance claim. |
| 2026-08-27 | MAP_001 encounter-to-world spawn/loot dependency integration | PARTIAL by broad-scope rules; real active `map001` encounter transitions, real Obsidian world spawn/loot nodes, deterministic Distress Pod and Leyline Monolith trigger/output records, bounded sampled coverage, explicit Glass Stalker/Void Reaper coverage and loot-context blockers, and admin-only Workbench preview verified | `d4d53ef` pushed to `origin/main` | Added `server/generators/map001EncounterDependencyGraph.ts` and `creator.dependencyGraph.map001EncounterPreview`; the adapter invokes `resolveMap001Encounter` for telegraph-to-active outputs and connects expected event species to sampled world spawn and loot nodes without changing encounter gameplay. Missing coverage or missing loot context remains required graph blockers; no mob/NPC definition or spawn is fabricated. Workbench adds a manual MAP_001 encounter → spawn/loot source with bounded sample controls and blocker reasons. Final validation passed `96` files / `369` tests, `pnpm check`, `git diff --check`, full `pnpm test -- --run`, and production build (only known analytics-placeholder and Babylon vendor chunk warnings). Browser evidence in `docs/map001-encounter-browser-evidence-2026-08-27.md` confirms player-only `/` and unauthenticated `DEVELOPER ONLY` Workbench gate. No authenticated graph E2E, database/storage write, encounter gameplay change, mob/NPC definition, runtime encounter/spawn/loot consumption, registry write, runtime publish/import/cache, or device/mobile acceptance claim. |
| 2026-08-27 | MAP_001 encounter-to-story gate dependency integration | PARTIAL by broad-scope rules; real MAP_001 encounter outputs, real `quest.progression` registry artifact, 20-quest prerequisite chain for planned `story-map-002`, explicit missing encounter-completion signal, future-map runtime boundary, and admin-only Workbench preview verified | `ce16106` pushed to `origin/main` | Added `server/generators/map001StoryGateDependencyGraph.ts` and `creator.dependencyGraph.map001StoryGatePreview`; the adapter reuses MAP_001 encounter outputs and the registered quest progression owner, checks that Obsidian Frontier is the only playable story map, and connects the 20 previous-map quest prerequisites to the MAP_001→MAP_002 gate. Because the encounter owner currently exposes boss-active but no boss-defeated completion signal, `map001-encounter-completion:boss-defeated` remains a required blocker; no completion is inferred and no planned map becomes runtime-importable. Workbench adds a manual MAP_001 encounter → story gate source with prerequisite/completion status and blocker reasons. Final validation passed `97` files / `373` tests, `pnpm check`, `git diff --check`, full `pnpm test -- --run`, and production build (only known analytics-placeholder and Babylon vendor chunk warnings). Browser evidence in `docs/map001-story-gate-browser-evidence-2026-08-27.md` confirms player-only `/` and unauthenticated `DEVELOPER ONLY` Workbench gate. No authenticated graph E2E, database/storage write, encounter completion gameplay, future-map runtime import, registry write, runtime publish/import/cache, or device/mobile acceptance claim. |
| 2026-08-27 | Story progression runtime-to-quest progression contract integration | PARTIAL by broad-scope rules; real story runtime state transitions, registered `quest.progression` artifact, contiguous 0–20 quest completion state, current quest matching, Obsidian Frontier playable boundary, planned `story-map-002` boundary, and admin-only Workbench preview verified | `d3e971f` pushed to `origin/main` | Added `server/generators/storyProgressionContractDependencyGraph.ts` and `creator.dependencyGraph.storyProgressionContractPreview`; the adapter builds runtime state through the existing `normalizeStoryProgressState`, `completeStoryQuest`, and `getStoryProgressSummary` owners, then compares it with the registered two-map `quest.progression` artifact. It validates the contiguous completed-quest prefix, current quest ID, the 20-quest playable chapter contract, and the next map remaining planned/runtime-import-denied. Preview is read-only and does not write player session state or enable future maps. Final validation passed `98` files / `377` tests, `pnpm check`, `git diff --check`, full `pnpm test -- --run`, and production build (only known analytics-placeholder and Babylon vendor chunk warnings). Browser evidence in `docs/story-progression-contract-browser-evidence-2026-08-27.md` confirms player-only `/` and unauthenticated `DEVELOPER ONLY` Workbench gate. No authenticated graph E2E, database/storage write, player story state mutation, future-map runtime import, registry write, runtime publish/import/cache, or device/mobile acceptance claim. |
| 2026-08-27 | Story runtime-to-map selection/cache policy integration | PARTIAL by broad-scope rules; real story progression contract, direct-route allow-list/fallback, `MAP_REGISTRY` lookup, cache owner guard, bounded requested map assessment, explicit future-map registry/approval blockers, and admin-only Thai Workbench preview verified | `a804979` pushed to `origin/main` | Added `server/generators/storyMapCachePolicyDependencyGraph.ts` and `creator.dependencyGraph.storyMapCachePolicyPreview`; adapter composes real story/direct-route/registry/cache owners without calling `prepareMapModule` or writing Cache Storage/localStorage. Obsidian Frontier is the only selection/cache-eligible map; future requests fallback to Obsidian but remain blocked by missing registry/runtime approval edges. `mapCache.ts` now denies future-map preparation and lookup before network/cache work. Final validation passed `99` files / `382` tests, `pnpm check`, `git diff --check`, and production build with known analytics-placeholder, non-module analytics script, and large Babylon vendor chunk warnings. Browser evidence in `docs/story-map-cache-policy-browser-evidence-2026-08-27.md` confirms player-only landing and unauthenticated `DEVELOPER ONLY` Workbench gate. No authenticated admin E2E, live DB/storage, runtime publish/import, generated assets, or device/mobile acceptance claim. |
| 2026-08-27 | Master authority และ OAuth account-security boundary | PARTIAL by broad-scope rules; server-side `user/gm/admin/master` role policy, requested master email bootstrap, Master-only authority routes, GM/admin creator-tool access, role revocation-to-user, additive role migration, and truthful OAuth-managed security surface verified | `c807f93` pushed to `origin/main` | Added `shared/authority.ts`, `drizzle/0008_authority_roles.sql`, server `masterProcedure`, `auth.authority.*` routes and `auth.securityStatus`, plus Thai `/authority-admin` and `/account-security` surfaces. Master email is configured through `MASTER_ADMIN_EMAIL` with the requested default and `OWNER_OPEN_ID` fallback; password is never stored by the game. Focused/full validation at this checkpoint passed `101` files / `392` tests, `pnpm check`, `git diff --check`, and production build with known analytics-placeholder/non-module script and large Babylon vendor warnings. Browser evidence in `docs/authority-browser-evidence-2026-08-27.md` confirms unauthenticated `MASTER ONLY` and OAuth account-security login boundaries. No live migration/db push, live user role update, authenticated Master E2E, OAuth email verification, provider password change/reset, email delivery or device acceptance claim. |
| 2026-08-27 | Immutable authority grant/revoke audit trail | PARTIAL by broad-scope rules; transactional role update + immutable audit event contract, Master-only audit route, Thai audit history UI, and protected/forbidden tests verified | `fe6e11d` pushed to `origin/main` | Added `authorityAuditEvents` schema/migration `0009_authority_audit_events.sql`; Master role changes now update `users` and write actor/target/action/fromRole/toRole/reason in one server transaction. `auth.authority.audit` is Master-only and AuthorityAdmin refreshes the member list and audit history after changes. Focused/full validation passed `101` files / `392` tests, `pnpm check`, `git diff --check`, and production build with known analytics-placeholder/non-module script and large Babylon vendor warnings. No live migration/db push, live role mutation, authenticated Master E2E, email/password provider operation, or device acceptance claim. |
| 2026-08-27 | Email-bound GM/Admin invitation and OAuth acceptance boundary | PARTIAL by broad-scope rules; Master-only invitation creation/list/revoke, normalized email matching, pending/expiry policy, protected OAuth-session acceptance, transactional role grant + authority audit, Thai invitation UI, and forbidden matrix verified | `ec6ec44` pushed to `origin/main` | Added `authorityInvitations` schema/migration `0010_authority_invitations.sql`, pure invitation acceptance policy, `auth.authority.invitations/invite/revokeInvitation`, and protected `auth.acceptAuthorityInvitation`. Invitations expire after 7 days and are accepted only when the authenticated OAuth email matches; no email/password is stored or sent by the game. Focused/full validation passed `101` files / `393` tests, `pnpm check`, `git diff --check`, production build with known analytics-placeholder/non-module script and large Babylon vendor warnings, and unauthenticated `/authority-admin` browser boundary evidence. No mail provider delivery, live migration/db push, authenticated Master/recipient E2E, provider email verification/password change/reset, or device acceptance claim. |
| 2026-08-27 | Story runtime-to-offline map-state namespace integration | PARTIAL by broad-scope rules; real story/cache/direct-route/MAP_REGISTRY owners, deterministic map+player namespace assessment, active-map-only offline write guard, future-map fallback/write blockers, admin-only Thai preview and player/unauthenticated boundary evidence verified | `5def3a4` pushed to `origin/main` | Added `storyOfflineMapStateDependencyGraph.ts`, `creator.dependencyGraph.storyOfflineMapStatePreview`, and a fail-closed `saveOfflineMapState` guard. Pure normalization can inspect planned data-only namespaces, but only Obsidian Frontier may write offline state; preview does not read/write IndexedDB or Cache Storage and does not prepare map modules. Focused/full validation passed with `102` test files / `398` tests, `pnpm check`, `git diff --check`, production build, and browser evidence in `docs/story-offline-map-state-browser-evidence-2026-08-27.md`. Known analytics-placeholder/non-module script and large Babylon vendor warnings remain. No live DB/storage, authenticated admin E2E, runtime import/cache preparation, or device acceptance claim. |

| 2026-08-27 | MAP_001 quest-to-gameplay-event dependency integration | PARTIAL by broad-scope rules; real quest progression objectives, known runtime action owners/payload contracts, deterministic event/target assessment, required missing-event/target blockers, admin/GM/master creator guard, Thai Workbench preview and player/unauthenticated boundary evidence verified | `f2fba0bd72a4708b88fea5804d603c206d7371b9` | Added `server/generators/questGameplayEventDependencyGraph.ts` and `creator.dependencyGraph.questGameplayEventPreview`. The read-only adapter samples MAP_001 objectives from `generateQuestProgression` and compares them with `block-break`, `block-place`, and `harvest-world-crop` contracts without submitting actions or changing story state. `visit`, `collect`, `talk`, `craft`, and `defeat` have no canonical runtime event owner; `mine` lacks exact mined/drop identity; `harvest` lacks `plantId` in the pending bridge; and `place-block` is not proven to match `itemDefinitionId`, so the sample remains invalid with required blockers and no quest completion/future-map unlock. Full validation passed `103` test files / `402` tests, `pnpm check`, `git diff --check`, and heap-limited production build with only known analytics-placeholder/non-module-script/Babylon chunk warnings. Browser evidence in `docs/quest-gameplay-event-browser-evidence-2026-08-27.md` confirms player-only landing and unauthenticated `DEVELOPER ONLY` Workbench gate. No authenticated creator E2E, DB/storage write, event emission, quest completion, runtime publish/import/cache, future-map unlock, or device/mobile acceptance claim. |

| 2026-08-27 | MAP_001 quest-reward-to-inventory capacity dependency integration | PARTIAL by broad-scope rules; real quest progression reward definitions, catalog item-definition and reward-instance owners, `inventorySystem/addItemToContainer` dry-run, bounded 40-slot capacity assessment, required inventory-capacity/dispatch/ability blockers, admin/GM/master creator guard, Thai Workbench preview and player/unauthenticated boundary evidence verified | `f9bd3db20d3c7a7044ae147fbb1d24f19ee65e15` | Added `server/generators/questRewardInventoryDependencyGraph.ts` and `creator.dependencyGraph.questRewardInventoryPreview`. The read-only adapter samples MAP_001 rewards, creates catalog reward instances and dry-runs them against a cloned 40-slot inventory without writing player state. Empty inventory accepts the sample instances, full inventory reports quantity-specific capacity blockers, and every reward remains unsupported until a canonical quest reward dispatch caller and ability runtime owner exist. Full validation passed `105` test files / `411` tests, `pnpm check`, `git diff --check`, and heap-limited production build after final integration, with only known analytics-placeholder/non-module-script/Babylon chunk warnings. Browser evidence in `docs/quest-reward-inventory-browser-evidence-2026-08-27.md` confirms player-only landing and unauthenticated `DEVELOPER ONLY` Workbench gate. No authenticated creator E2E, DB/storage write, reward emission, inventory mutation, ability unlock, quest completion, runtime publish/import/cache, future-map unlock, or device/mobile acceptance claim. |

| 2026-08-27 | MAP_001 quest-reward-to-item/ability runtime dependency integration | PARTIAL by broad-scope rules; real quest reward definitions, catalog item-definition lookup, reward-instance factory, reward handler owner, missing quest-reward dispatch bridge, missing ability runtime owner, admin/GM/master creator guard, Thai Workbench preview and player/unauthenticated boundary evidence verified | `74d4efda2c9a96ab565e4d079d4ce09b5530058b` | Added `server/generators/questRewardRuntimeDependencyGraph.ts` and `creator.dependencyGraph.questRewardRuntimePreview`. The read-only adapter samples MAP_001 rewards from `generateQuestProgression`, confirms the existing catalog can represent the sample item rewards through `createMapRewardInstance`, and keeps the missing bridge from `completeStoryQuest` to `ArcaneFrontier/rewardHandler` explicit. The final quest ability `ability.story.001` has no runtime owner, so no ability unlock is inferred. Full validation passed `104` test files / `406` tests, `pnpm check`, `git diff --check`, and heap-limited production build with only known analytics-placeholder/non-module-script/Babylon chunk warnings. Browser evidence in `docs/quest-reward-runtime-browser-evidence-2026-08-27.md` confirms player-only landing and unauthenticated `DEVELOPER ONLY` Workbench gate. No authenticated creator E2E, DB/storage write, reward emission, inventory mutation, ability unlock, quest completion, runtime publish/import/cache, future-map unlock, or device/mobile acceptance claim. |

| 2026-08-27 | MAP_001 quest-reward dispatch transaction contract | PARTIAL by broad-scope rules; pure atomic completion+item transition, deterministic reward provenance, 40-slot capacity gate, future-map rejection, missing reputation/ability/persistence owner blockers, admin/GM/master preview route, Thai Workbench integration and player/unauthenticated boundary evidence verified | `333078e3f78e3647ba6643f98b76493dc982b726` | Added `client/src/game/systems/questRewardDispatchSystem.ts`, `server/generators/questRewardDispatchDependencyGraph.ts` and pure dispatch/graph tests. The transition reuses `completeStoryQuest`, `createMapRewardInstance` and `addItemToContainer`; it returns unchanged input state on any later reward blocker, does not call `saveSession`, IndexedDB, gameplay emitters or future-map import, and exposes only a read-only `creator.dependencyGraph.questRewardDispatchPreview`. Full validation passed `107` test files / `423` tests, `pnpm check`, `git diff --check`, heap-limited production build, and browser evidence in `docs/quest-reward-dispatch-browser-evidence-2026-08-27.md`; only known analytics-placeholder/non-module-script/Babylon chunk warnings remain. No authenticated creator E2E, live DB/storage, runtime reward emission, inventory mutation, ability/reputation unlock, quest completion caller, future-map unlock or device/mobile acceptance claim. |
| 2026-08-27 | MAP_001 quest-reward pending-action persistence contract | PARTIAL by broad-scope rules; pure accepted-result serialization, bounded server validator, semantic `questId`↔`questOrder` pairing, deterministic event/instance provenance, admin/GM/master preview boundary and player/unauthenticated browser boundary verified | `05b27c1a16e51f741256d7b08d57e5ee579bb9eb` + `330593c84bec473fddd6c255171270c73291571a` | Added `client/src/game/systems/questRewardPendingAction.ts`, `HomeAction` union support and `isSafeQuestRewardDispatchPayload`. The helper accepts only an accepted pure dispatch result and produces a JSON-safe bounded action; the server validator enforces Obsidian/MAP_001 allow-lists, one-to-one event/instance IDs, bounds and exact questId↔questOrder pairing. Focused validation passed `5` files / `60` tests; final full validation passed `108` files / `428` tests, `pnpm check`, `git diff --check`, heap-limited production build, and player/unauthenticated browser smoke with evidence in `docs/quest-reward-persistence-browser-evidence-2026-08-27.md`. No gameplay caller, session/IndexedDB/network persistence, reward emission, ability/reputation unlock, authenticated creator E2E, live DB/storage, future-map unlock or device/mobile acceptance claim. |
| 2026-08-27 | MAP_001 quest-reward persistence/sync gate integration | PARTIAL by broad-scope rules; existing pending action now passes through a fail-closed client offline queue gate and server sync allow-list without adding gameplay reward delivery | `f55b154959409c8018332970c45a4d76f562bbfa` | Added `isSafeQuestRewardPendingAction` and `selectQueueableSessionActions` so malformed `quest-reward-dispatch` actions are rejected with `rejectedIds`; `isSafeSyncAction` connects `isSafeQuestRewardDispatchPayload` to `writeGameSyncBatch`. Focused validation passed `3` files / `16` tests; full validation passed `109` files / `431` tests, `pnpm check`, `git diff --check` and heap-limited production build. No gameplay caller, reward grant, quest completion, persistence runtime E2E, live DB/storage, ability/reputation unlock, future-map unlock or device/mobile acceptance claim. |
| 2026-08-27 | Runtime capability advisory contract | PARTIAL by broad-scope rules; bounded one-time normalization of WebGL/WebGL2/WebGPU, CPU, memory, storage, touch and viewport signals with conservative/high heuristic advice verified | `937441a` (`937441acb39f28f9d9e0e9f8e41354b23cfe9a3`) | Added pure `runtimePerformanceCapability.ts` and `server/runtimePerformanceCapability.test.ts`. The contract clamps unknown/unsafe values, fails closed when graphics capability is missing, exposes high-tier advice only for explicit heuristic signals, and states `deviceBenchmark:false`, `adaptiveTiering:false`, `autoApplied:false`, `renderLoopCoupled:false` and `networkPersistence:false`. Full validation passed `110` files / `439` tests, `pnpm check`, `git diff --check` and heap-limited production build with known analytics-placeholder/non-module-script/Babylon chunk warnings. No runtime wiring, automatic tier change, actual WebGL/device benchmark, mobile acceptance, player UI or future-map effect is claimed. |
| 2026-08-27 | Item/plant asset provenance dependency graph | PARTIAL by broad-scope rules; sampled item/plant asset references are checked against the active `arcane-frontier-voxel-pixel` manifest and project credit records, with missing manifest/credit bindings preserved as required blockers | `83bd23e` (`83bd23e7a79ce87f626876b41d58f43cabba83a7`) | Added pure `server/generators/assetProvenanceDependencyGraph.ts` and `server/assetProvenanceDependencyGraph.test.ts`. The graph validates active-pack identity, deterministic manifest hash, bounded samples, manifest entry bindings, credit/provenance records and read-only policy; actual starter manifest coverage is inspected from repository JSON, while the existing credit catalog intentionally leaves many per-asset credit bindings unresolved. Focused `1` file / `5` tests, full `111` files / `444` tests, `pnpm check`, `git diff --check` and heap-limited production build passed. No PNG/GLB generation, creator route/UI change, runtime import/publish/cache effect, asset replacement, DB/storage write or mobile acceptance claim. |
| 2026-08-27 | Animation motion policy | PARTIAL by broad-scope rules; pure distance/visibility/reduced-motion policy with full/reduced/sleep/static modes, bounded update cadence and clip/profile reuse advice verified | `e8d4a9d` (`e8d4a9dc4be85a88cc167a84c0f01d569bcc7755`) | Added `client/src/game/systems/animationMotionPolicy.ts` and `server/animationMotionPolicy.test.ts`. The policy normalizes unknown tiers through existing performance budgets, sleeps invisible/out-of-radius entities, reduces visible far/reduced-motion updates, keeps dead state static, and reports explicit non-claims for binary generation, render-loop generation, skeleton retargeting, wind simulation, adaptive tiering, device benchmark and player mutation. Focused `2` files / `11` tests, full `112` files / `452` tests, `pnpm check`, `git diff --check` and heap-limited production build passed. No GameCanvas caller, binary animation asset, skeleton/retarget implementation, wind system, player UI, automatic runtime mutation, device/mobile acceptance or future-map effect is claimed. |
| 2026-08-27 | Category-specific item detail facts | PARTIAL by broad-scope rules; pure facts for usage/stack, plant soil/effect, placeable block and tool tag are exposed from canonical definitions, while weapon attack damage remains explicit unavailable because no owning field exists | `ea08e4a` (`ea08e4ad2a943dca19981c83669359c449302f5a`) | Added `getItemCategoryDetail` to `client/src/game/systems/itemDetailSystem.ts` and regression coverage in `server/itemDetailSystem.test.ts`. The contract preserves optional instance provenance/enhancement, emits category-specific facts only from fields present in `ItemDefinition`, and returns an explicit unavailable attack-damage fact with a reason instead of inventing a combat number. Focused `3` files / `13` tests, full `112` files / `455` tests, `pnpm check`, `git diff --check` and heap-limited production build passed. No ArcaneFrontier UI integration, persistence caller, runtime mutation, fabricated combat stat or future-map effect is claimed. |
| 2026-08-27 | Plant ecology policy | PARTIAL by broad-scope rules; pure catalog-driven soil/biome compatibility, four-stage lifecycle metadata, mature-only harvest semantics and bounded fictional water/nutrient/pest/season factors are verified, while runtime ecology owners remain absent | `9720ee0` (`9720ee09c80c6e41d2993ac75aa1ddfef8e00a0c`) | Added `client/src/game/systems/plantEcologyPolicy.ts` and `server/plantEcologyPolicy.test.ts`. The policy resolves plant or seed IDs through the real 300-entry `PLANT_CATALOG`, clamps fictional factors to `[0,1]`, returns explicit incompatible/missing-factor/eligible statuses, caps effect power/radius, and names missing nutrient/pest/season owners. Full validation passed `114` files / `466` tests, `pnpm check`, `git diff --check` and heap-limited production build. No real ecology simulation, worldFarmSystem caller, gameplay mutation, persistence, player UI, asset generation or future-map effect is claimed. |
| 2026-08-27 | Discovered-only Codex contract | PARTIAL by broad-scope rules; pure canonical discovery normalization, deterministic ordering, empty state, duplicate/unknown rejection and category filtering are verified, while player event wiring and durable discovery persistence remain absent | `3decb18` (`3decb180d1cae1e9aea55126e01c00c3a34919f4`) | Added `client/src/game/systems/codexDiscoveryContract.ts` and `server/codexDiscoveryContract.test.ts`. The contract exposes only IDs present in `CODEX_ENTRIES`, returns explicit rejection metadata, preserves category counts and supports a pure union transition without IndexedDB/session/network writes. Full validation passed `115` files / `472` tests, `pnpm check`, `git diff --check` and heap-limited production build. No Codex UI edit, discovery gameplay event caller, persistence owner, asset generation or future-map effect is claimed. |
| 2026-08-27 | Credits provenance presentation contract | PARTIAL by broad-scope rules; pure grouping of `ASSET_CREDITS` into project-original/license-verified/reference-only/needs-review records, source preservation and distribution eligibility are verified, while CreditsSheet contract integration remains absent | `c3a859d` (`c3a859dd215f128ff32a52d5aeab86d8a08fc5c4`) | Added `client/src/game/systems/creditsPresentationContract.ts` and `server/creditsPresentationContract.test.ts`. The contract rejects malformed/duplicate records, preserves attribution/source URL, separates reference-only entries from runtime distribution and is read-only. Focused validation passed `2` files / `10` tests; full validation passed `116` files / `477` tests, `pnpm check`, `git diff --check` and heap-limited production build. No CreditsSheet/player UI integration, publishing, asset replacement or persistence claim is made. |
| 2026-08-27 | Mobile viewport policy | PARTIAL by broad-scope rules; pure bounded normalization of viewport dimensions, orientation conflicts, safe-area insets, touch/fullscreen signals and landscape/portrait layout advice are verified, while CSS/UI wiring and real-device acceptance remain absent | `6745751` (`6745751630589edf84c7be6582e4f4721b9e4bf2`) | Added `client/src/game/systems/mobileViewportPolicy.ts` and `server/mobileViewportPolicy.test.ts`. The policy clamps unsafe values, fails closed on orientation conflict, keeps unknown capabilities explicit and declares no CSS safe-area application, orientation lock, fullscreen guarantee, real-device/WebView acceptance or player-state write. Focused `3` files / `14` tests, full `117` files / `483` tests, `pnpm check`, `git diff --check` and heap-limited production build passed. No `index.css`, `client/index.html`, GameCanvas, player UI or future-map effect is claimed. |
| 2026-08-27 | Codex UI boundary integration | PARTIAL by broad-scope rules; existing player CodexSheet now consumes the canonical discovered-only snapshot and fails closed for invalid discovery input, while event ownership and durable discovery persistence remain absent | `1085ac2` (`1085ac2` pushed to `origin/main`) | Updated `client/src/pages/ArcaneFrontier.tsx` to use `createCodexDiscoverySnapshot` and canonical category counts without changing the existing `discoveredItemIds` session/persistence owner. Focused `server/codexDiscoveryContract.test.ts` passed `1` file / `6` tests; full validation passed `117` files / `483` tests, `pnpm check`, `git diff --check` and heap-limited production build. Browser sandbox opened Codex from player landing and showed discovered-only categories/detail; evidence in `docs/evidence/codex-ui-boundary-browser.md`. No discovery event creator, IndexedDB/network write, mobile/device acceptance, creator control or future-map effect is claimed. |
| 2026-08-27 | Credits UI boundary integration | PARTIAL by broad-scope rules; existing player CreditsSheet now consumes canonical credits presentation sections and hides all entries on invalid contract input, while durable registry/license approval/publish and mobile acceptance remain absent | `f3eea8c` (`f3eea8c80ee86f72ecf2f726ea8aaff5623c9a55` pushed to `origin/main`) | Updated `client/src/pages/ArcaneFrontier.tsx` to use `createCreditsPresentation(ASSET_CREDITS)`, render section metadata/status/review labels/source links and preserve the runtime/reference-only boundary. Focused `server/creditsPresentationContract.test.ts` passed `1` file / `5` tests; full validation passed `117` files / `483` tests, `pnpm check`, `git diff --check` and heap-limited production build. Browser sandbox opened CreditsSheet from player landing and showed project/reference sections and source links; evidence in `docs/evidence/credits-ui-boundary-browser.md`. No asset bytes, publish, persistence, network write, creator control, license approval, mobile/device acceptance or future-map effect is claimed. |
| 2026-08-27 | Item detail UI boundary integration | PARTIAL by broad-scope rules; existing long-press ItemDetailSheet now consumes canonical category detail facts, while combat damage ownership and broader mobile acceptance remain absent | `bb51a55` (`bb51a5572702e023854b1d3d70c5f730a559048c` pushed to `origin/main`) | Updated `client/src/pages/ArcaneFrontier.tsx` to call `getItemCategoryDetail(definition, instance)` and render usage/stack plus plant soil/effect, placeable block and tool-tag facts when owned by the canonical definition. Weapon attack damage renders `ยังไม่มีข้อมูล` with the source-owned reason and no fabricated number. Focused `server/itemDetailSystem.test.ts` passed `1` file / `6` tests; full validation passed `117` files / `483` tests, `pnpm check`, `git diff --check` and heap-limited production build. Browser sandbox opened the Vault with Player ID `DetailProof`, simulated the existing 3.6-second long-press on `Aether Blade 001`, and observed the unavailable-damage message; evidence in `docs/evidence/item-detail-ui-boundary-browser.md`. No combat stat, persistence, asset generation, creator control, authenticated E2E, real-device acceptance or future-map effect is claimed. |
| 2026-08-27 | S-03 target FPS disclosure | PARTIAL by broad-scope rules; player settings now explicitly label 120 FPS as high-refresh advisory/non-guarantee, while actual device/FPS acceptance remains absent | `7dd5c46` (`7dd5c460a5b7bdb54447a58a5fca5d0ada1b3bd7` pushed to `origin/main`) | Updated `client/src/pages/ArcaneFrontier.tsx` option copy and in-map note. Browser sandbox entered Obsidian Frontier and selected `120 FPS`; UI showed `high-refresh advisory · ไม่รับประกัน` plus the note that FPS is a game target, not a device performance test. Full validation passed `117` files / `483` tests, `pnpm check`, `git diff --check` and heap-limited production build. Evidence in `docs/evidence/s03-target-fps-disclosure-browser.md`. No FPS benchmark, device/WebView acceptance, runtime normalization change, map/cache policy change or production claim is made. |
| 2026-08-27 | G-02 spatial validator status reconciliation | PARTIAL by broad-scope rules; current main already contains bounded Obsidian spatial rules/repair and a pure dependency graph, but universal map coverage and exported-artifact acceptance remain absent | `50f17a1` (`50f17a17f171429ab2f3a7d961659e79ea95921e` pushed to `origin/main`) | Reconciled stale G-02 prose from `ไม่มี universal validator`/`PENDING` to the verified bounded scope: `worldSpatialConstraints.ts` owns bounds/surface/height/slope/water/overlap/grounding/support rules and repair, while `worldSpatialDependencyGraph.ts` has focused `2` files / `14` tests and full historical validation `113` files / `460` tests. No new code, universal coverage, player generator UI, future-map write or production export acceptance is claimed. |
| 2026-08-27 | M-01 actual landscape CSS boundary | PARTIAL by broad-scope rules; `index.css` now applies safe-area fallback variables and bounded landscape sizing/overflow rules to player HUD, controls, settings modal, vault and footer, while real-device/WebView acceptance remains absent | `9c55942` (`9c559427ba1e4a29f4ae253b66c3432f2619b7a6` pushed to `origin/main`) | Updated `client/src/index.css` only. Full validation passed `117` files / `483` tests, `pnpm check`, `git diff --check` and heap-limited production build. Chromium viewport screenshots at `320x240`, `390x240`, `430x240` and `768x240` showed the Obsidian HUD/control/footer within the viewport; evidence in `docs/evidence/m01-css-viewports.md` and `docs/evidence/m01-css-viewports/`. No `client/index.html`, `ArcaneFrontier.tsx`, `GameCanvas`, runtime policy, fullscreen guarantee, touch/device acceptance or future-map effect was added. |
| 2026-08-27 | B-02 block physics contract test boundary | PARTIAL by broad-scope rules; the existing `blockPhysicsSystem.ts` contract is now locked by focused tests for solid slab bounds/blocking, thin non-solid pass-through, canonical cactus hazard contact for player/creature, cooldown and broken-block exclusion, while broad world-family coverage and deterministic device/gameplay acceptance remain absent | `2998e34` (`2998e34` pushed to `origin/main`) | Added `server/blockPhysicsSystem.test.ts` with 6 focused tests. Full validation passed `118` files / `489` tests, `pnpm check`, `git diff --check` and heap-limited production build. Browser smoke entered Obsidian Frontier from the player route and observed the HUD/control boundary; evidence in `docs/evidence/b02-block-physics-browser.md`. No new hazard, damage value, gravity solver, persistence, future-map behavior, creator control or real-device acceptance was added. |
| 2026-08-27 | B-03 worker support/gravity dependency audit merge | PARTIAL by broad-scope rules; AI-2 PR #30 added a pure dependency graph over canonical `OBSIDIAN_BLOCKS` and real physics owner, with fail-closed blockers and audit-only runtime policy; runtime mutation, universal gravity and persistence remain absent | `6ef111c` (merge commit for PR #30, pushed to `origin/main`; worker implementation `255a351aa4b02330dc989643b9dd8039a3d0bfdb`) | Reviewed exact files `server/generators/blockSupportGravityDependencyGraph.ts`, `server/blockSupportGravityDependencyGraph.test.ts`, `docs/AI_HANDOFF_B03_SUPPORT_GRAVITY_REPORT.md` on detached worktree. Focused PR suite passed `1` file / `5` tests; full validation passed `118` files / `488` tests, `pnpm check`, `git diff --check` and heap-limited build. Merge did not touch block runtime, map/cache policy, persistence, creator controls or future-map behavior. |
| 2026-08-27 | M-01 actual landscape CSS boundary | PARTIAL by broad-scope rules; `index.css` now applies safe-area fallback variables and bounded landscape sizing/overflow rules to player HUD, controls, settings modal, vault and footer, while real-device/WebView acceptance remains absent | `9c55942` (`9c559427ba1e4a29f4ae253b66c3432f2619b7a6` pushed to `origin/main`) | Updated `client/src/index.css` only. Full validation passed `117` files / `483` tests, `pnpm check`, `git diff --check` and heap-limited production build. Chromium viewport screenshots at `320x240`, `390x240`, `430x240` and `768x240` showed the Obsidian HUD/control/footer within the viewport; evidence in `docs/evidence/m01-css-viewports.md` and `docs/evidence/m01-css-viewports/`. No `client/index.html`, `ArcaneFrontier.tsx`, `GameCanvas`, runtime policy, fullscreen guarantee, touch/device acceptance or future-map effect was added. || 2026-08-28 | B-04 worker tool-aware block-drop dependency audit merge | PARTIAL by broad-scope rules; AI-2 PR #31 added a pure fail-closed graph over canonical `OBSIDIAN_BLOCKS`, block-item links, tool tags and existing `resolveBlockBreak` behavior. Correct/wrong tool outcomes are audited without changing runtime; durability owner, universal runtime integration, persistence and device acceptance remain absent | `835296a` (merge commit for PR #31, pushed to `origin/main`; worker implementation `ed3f70892b76c054166e54b44bf47a8993045112`) | Reviewed exact files `server/generators/blockDropToolDependencyGraph.ts`, `server/blockDropToolDependencyGraph.test.ts`, `docs/AI_HANDOFF_B04_BLOCK_DROP_REPORT.md` on detached worktree. Focused PR suite passed `1` file / `5` tests; full validation passed `118` files / `488` tests, `pnpm check`, `git diff --check` and heap-limited build. Merge preserved Obsidian-only/read-only boundaries and did not add drop mutation, inventory mutation, durability decrement, persistence, creator controls or future-map behavior. |

| 2026-08-28 | B-05 worker block placement persistence audit merge | PARTIAL by broad-scope rules; AI-2 PR #33 added a pure fail-closed graph over canonical `RUNTIME_MAP_ID`, map+player state identity, coordinate overrides, tombstone/replacement hydration and generated-mesh non-persistence. It does not open IndexedDB or mutate runtime state; authoritative runtime/storage E2E, reconnect/cross-device and production persistence remain absent | `87760a0` (merge commit for PR #33, pushed to `origin/main`; worker implementation `fc5ff2feae4208539ecc39dd2514a645c687f8e1`) | Reviewed exact files `server/generators/blockPersistenceDependencyGraph.ts`, `server/blockPersistenceDependencyGraph.test.ts`, `docs/AI_HANDOFF_B05_BLOCK_PERSISTENCE_REPORT.md` on detached worktree. Focused PR suite passed `1` file / `6` tests; full validation passed `118` files / `489` tests, `pnpm check`, `git diff --check` and heap-limited build. Merge preserved the single playable/cacheable/offline-write `obsidian-frontier` policy and did not add IndexedDB/DB writes, future-map writes, service-worker changes, creator controls, runtime mutation or device acceptance. |

### Checkpoint 2026-08-26 — Obsidian block-first interaction slice

The focused block tests verify independent coordinate keys, deterministic Obsidian floor reads, correct pickaxe drops, wrong/hand no-drop destruction, solid support, occupied-cell rejection, 64-cap placement and one-item atomic stack consumption, legacy override normalization, and map+player state identity. Server sync tests verify that block actions are accepted only for bounded Obsidian payloads and reject future-map, invalid-module, and out-of-range inputs. Browser proof with fresh local Player ID `BlockProof` showed four quick slots including `Aether Pickaxe`, correct tool selection, a correct-tool drop recorded in localStorage, wrong-tool destruction without a second drop, a successful placement with a `block-place` action, and IndexedDB overrides surviving direct-route reload. The existing local dev environment has no authenticated session cookie, so sync-attention messaging appeared during browser proof; it does not change the local block rule or persistence evidence. This checkpoint does not claim completion of the broader B-01/B-03/B-04/B-05/B-06 requirements.

### Checkpoint 2026-08-26 — Obsidian camera/settings slice

The camera/settings slice separates app-wide controls from map-local controls. Global settings retain quality, effect, audio, touch, and reduced-motion values; the in-map sheet contains camera mode, block view distance, and target FPS. The Babylon scene creates one `UniversalCamera` for first-person and one `ArcRotateCamera` for overhead/side, updates them through `arcane-control` events, and keeps the React canvas mounted while settings change. First-person hides the hero mesh and follows player position/rotation; overhead and side restore the hero mesh and use distinct framing. The in-map settings record is normalized for legacy state and persisted with the existing `[mapId+playerId]` IndexedDB key.

Focused camera and offline-state tests cover defaults, all allowed option lists, malformed legacy fallback, valid custom settings, and namespace identity. Full validation passed with `40` test files and `130` tests, `pnpm check`, `git diff --check`, and production build. Browser evidence in `docs/phase6-browser-notes.md` verified direct future-map fallback to Obsidian, selection and visual change for first-person, side, and overhead, the 50-block bound, and reload hydration of `overhead / 50 / 60`. This checkpoint does not claim FPS limiting, fine-grained 5–50 streaming, mobile/device acceptance, pause/focus semantics, or completion of S-01/S-02/S-03/S-04.

### Checkpoint 2026-08-26 — Obsidian chest/storage slice

The scoped Obsidian chest slice uses a deterministic `storage.obsidian.chest` world record at the start area. The pure storage engine keeps a fixed 27-slot chest, validates a 40-slot carry limit for withdrawal, moves full `ItemInstance` objects without stack splitting, preserves provenance, and emits validated `storage-deposit`/`storage-withdraw` offline actions. Legacy `OfflineMapState` values are normalized with `worldStorageById`, and Dexie continues to use its composite `[mapId+playerId]` key. The Babylon scene opens the chest before generic block/resource interaction; React keeps the scene alive through latest-prop callbacks and persists the newest block/farm/storage state together.

Focused tests cover chest capacity, carry-full rejection, atomic no-mutation failures, provenance preservation, malformed/duplicate normalization, map-scoped action payloads, legacy map-state defaults, and sync rejection for future maps or invalid chest/slot/quantity identifiers. Browser evidence in `docs/phase5-browser-proof-notes.md` verified the 27/40 UI, deposit, withdraw, reload round trip, and a second deposit/reload/reopen showing the pickaxe still in chest slot 1. The unauthenticated development environment still displays its existing integrity/sync attention overlay; this does not change the local transfer result. This checkpoint does not claim the broader B-06/B-07 requirements complete.

### Checkpoint 2026-08-26 — Obsidian world farming slice

The deterministic Plant Catalog Generator checkpoint `5e4bdff` provides 300 validated data profiles with deterministic soil/biome links and capped fictional effects. The farming code checkpoint `afe0620` adds a map-local Obsidian plot state keyed through the existing `[mapId + playerId]` IndexedDB identity, planting and harvesting plans, stage-aware elapsed-time evaluation, accepted-action inventory semantics, provenance-backed reward instances, and validated offline sync payloads. The runtime uses crop block records and refreshes visuals without restarting Babylon for each interaction.

Focused tests cover generator count/link validation, soil and biome rejection, atomic planting, elapsed growth, mature-only harvest, reward provenance, capped repel/effect behavior, state normalization, and farming sync boundaries. Full validation passed with `37` test files and `116` tests, `pnpm check`, `git diff --check`, and production build. Browser evidence from the fresh `BlockProof` profile confirmed seed planting, IndexedDB plot persistence, elapsed-time maturity after reload, and a keyboard `E` harvest producing `material-001` with `world-harvest-obsidian-frontier-farm-plot-01-1787755986238` provenance while clearing the plot. A first pointer attempt was intercepted by the integrity relay and hit a nearby block; it is explicitly excluded from the harvest claim. This checkpoint does not claim the broader F-01/F-02/F-04/F-05/F-06/F-07 requirements complete.


## Supplemental owner material read 2026-08-26

เจ้าของส่งไฟล์ `pasted_content.txt` ถึง `pasted_content_16.txt` เพื่อใช้เป็นแนวทางเสริม และอ่านครบแล้ว ไฟล์บางชุดเป็นสำเนาเนื้อหาเดียวกัน จึงสรุปเป็นข้อกำหนดรวมโดยไม่ถือว่าเป็น source code หรือหลักฐานว่างานมีอยู่จริง

| กลุ่มเนื้อหา | ข้อกำหนดที่ต้องเก็บไว้ใช้ | ลำดับ/สถานะ |
|---|---|---|
| Product direction | A_Survival ควรมีโลก Block/Voxel ที่ความสัมพันธ์ระหว่าง biome, environment, plants, resources, mobs, items และ gameplay เป็นจุดต่าง; ระบบซับซ้อนได้แต่ onboarding ต้องง่าย | เก็บเป็น design direction; ยังไม่ใช่ acceptance claim |
| World instance/network | แยก Map ออกจาก World Instance; รองรับ Local, LAN และ Online Friend Session ผ่าน network abstraction/provider ในอนาคต; sync เฉพาะ instance เดียวกันและ permissions ต้องแยก see/join/build/destroy/fight/invite | deferred หลัง current offline Obsidian slice; ยังไม่มี multiplayer implementation |
| Runtime performance rule | สิ่งที่คำนวณล่วงหน้าได้ต้อง pre-generate/store/cache/reuse; ห้าม generate world/texture/model/animation หรือ recalculate definitions ทุก frame; runtime ทำเฉพาะ input → state → necessary simulation → visible objects → rendering | performance profile + bounded QA telemetry foundation ถูก implement แล้ว; ยังไม่ใช่ unified performance tool |
| Runtime architecture | ต้องใช้ asset registry, chunk streaming, frustum/distance culling, occlusion เมื่อเหมาะสม, LOD, pooling, spatial partitioning, sleep/wake และ distance-based AI/animation/physics; generator ต้องทำ background/pre-generation และไม่บล็อก render loop | current source มี profile budget, distance updates, sleep/wake และ QA telemetry foundation บางส่วน; ยังไม่มี full unified controller |
| Device/performance limits | ตรวจ capability จริงของ CPU/GPU/WebGL/WebGPU/storage/touch/browser และใช้ short benchmark; มี ULTRA LOW ถึง ULTRA tiers, hysteresis และลด render/mob/animation/physics/chunk/shadow/particle/texture/LOD/effect ตาม runtime | มี explicit low/balanced/high policy และ browser cadence observation เท่านั้น; ยังไม่มี capability detection, device benchmark หรือ ULTRA tier |
| Voxel/data/asset separation | แยก Engine/Data/Assets; block ใช้ definition/ID และ texture atlas/face mapping; mob/plant/item รองรับ model/texture หลายรูปแบบ; asset ต้องโหลดเฉพาะที่จำเป็นและ deduplicate/compress/stream ไม่ฝังทุกอย่างใน HTML | ผูกกับ V-03/G-04/T-01; current manifest/loader เป็นเพียงฐานบางส่วน |
| Universal item system | Item ต้องสัมพันธ์กับ world/resource/material/crafting/build/combat/progression; รองรับ weapon families, role/trade-off, power budget, counters, elements, damage/status, capped stacking, synergy, set, durability และ economy | ผูกกับ T-03; current catalog/combat เป็นฐานบางส่วน ไม่เริ่มก่อนคิว |
| Universal plant/ecology | Plant/Tree Engine กลางรองรับอย่างน้อย 300 species แบบ data-driven พร้อม biome/soil/terrain/environment rules, staged growth, offline elapsed-time simulation, fertilizer/nutrients/health/stress/disease/pests/pollination/season/weather/genetics/processing/trade ในระยะยาว | current phase 4 มีเฉพาะ minimal Obsidian world farm และ deterministic catalog foundation; universal engine ยัง PENDING |
| Generator ecosystem | เพิ่ม Quest, Structure, Dungeon, Loot, Crafting, Economy/Balance, Audio/Sound, Weather/Environment, Vegetation, Crop/Farming, Simulation/Auto-play Tester, Performance Profiler, Save/World Migration และ AI Game Design Assistant โดยใช้ Common Generator API/Asset Schema เดียวกัน | ผูกกับ T-04/T-06/T-07 และ phase 8–14; backend/build pipeline only, no player generator UI |
| Structure/Building material | Structure มีหลายระดับ Object → Building → Compound → Settlement → Landmark; building asset ต้องมี metadata, biome/terrain/climate/settlement/mob rules, footprint/height/space/road constraints, interior blueprints, decoration and NPC/mob linkage; placement ต้อง score แล้ว reject/repair หากน้ำ/ลาดชัน/ชน/จม/ลอย/ทางเข้าไม่ผ่าน และใช้ library/rotation/mirror/variation แทนการสร้าง asset ซ้ำเป็นพันชิ้น | อ่าน `pasted_content.txt` ครบ 567 บรรทัด; เป็น design specification ที่ผูกกับ T-06 และยังไม่มี implementation | deferred; backend/data tool only, no player generator UI |
| Procedural animation/entity scale | เพิ่ม Motion Library, skeleton templates, seeded procedural variation, animation composition/LOD, environmental wind motion และแยก world scale/entity scale/model scale/texture resolution; entity ไม่จำเป็นต้องมีขนาด 1 block | ผูกกับ T-02 และ phase 10; ยังไม่ implement |
| Web/WebView constraints | ต้องคำนึงถึง JS/WebGL/WebView/RAM/thermal/network/offline limits, server authority และ interest management ในอนาคต; ลำดับที่เสนอคือ prototype → small world → chunk → mobs → multiplayer → benchmark → scale | เป็น architecture guidance; ห้ามอ้าง mobile FPS/thermal/real-device success โดยไม่มีหลักฐาน |

ไฟล์ที่มีสาระซ้ำกัน ได้แก่ `pasted_content_4.txt`/`pasted_content_10.txt`, `pasted_content_5.txt`/`pasted_content_11.txt`, `pasted_content_6.txt`/`pasted_content_12.txt`, `pasted_content_7.txt`/`pasted_content_13.txt` และ `pasted_content_9.txt`/`pasted_content_14.txt`; การซ้ำไม่เพิ่มสถานะ implementation

การอ่านชุดนี้ยังคงหลัก dependency gates แต่ลำดับปัจจุบันได้เดินมาถึง generator/toolchain แล้ว: หลัง Obsidian farming, chest และ camera/settings ได้มีหลักฐาน จึงสร้าง Common Generator, Structure, Content Catalog, Universal Item และ Texture/Asset Pack Builder ตามลำดับ; ทุกหน่วยต้องมี test, check, evidence, matrix update และ commit/push แยกกัน

### Requirements reconciliation audit — 2026-08-26

ตรวจต้นฉบับจากพื้นที่อัปโหลดครบ `A_Survival_Toolkit_Master.txt` และ `pasted_content.txt` ถึง `pasted_content_16.txt` รวม 18 ไฟล์ โดยอ่านส่วนท้ายของไฟล์ที่ยาวเกินผลแสดงครั้งแรกเพิ่มเติมแล้ว ผล audit และรายการ duplicate/ข้อขัดแย้งระหว่างเอกสารอยู่ที่ `docs/REQUIREMENTS_RECONCILIATION_2026-08-26.md` และ `docs/uploaded-docs-audit-notes-2026-08-26.md`

การตรวจ source จริงปัจจุบันพบ generator/tool foundations ใน `server/generators/` ได้แก่ Common Generator API, Structure, Content Catalog, Universal Item, Texture Pack และ Animation Profile รวมถึง `server/creatorRouter.ts` สำหรับ admin-only previews และ `server/creatorArtifactRegistry.ts` สำหรับ durable texture metadata/storage references; มี `questProgressionGenerator` และ local `storyProgressionSystem` เป็น story foundation ล่าสุด. ยังไม่มี Quest objective runtime ครบ, Dungeon/Loot/Crafting/Economy/Audio/Weather/Vegetation/Profiler/Migration/Orchestrator ที่ครบ, full Procedural Animation/Motion Generator, world-instance/network implementation, unified adaptive performance controller, creator-specific role หรือ generic register/export persistence ของทุก domain. สถานะจึงคงเป็น `PARTIAL` หรือ `PENDING` ตามแถวด้านบน และไม่ยกระดับจากการอ่านเอกสารเพียงอย่างเดียว

`pnpm check`, full suite และ production build ถูก rerun ต่อเนื่องหลัง creator, plant, animation, story และ item-detail checkpoints; หลักฐานล่าสุดของ story/item unit คือ `64` test files / `250` tests ก่อน browser smoke. Browser evidence ใน `docs/item-detail-browser-evidence-2026-08-27.md` ยืนยัน player landing/lobby, Obsidian-only map selector, game HUD, long-press item detail และ short hotbar detail; ไม่มี authenticated creator browser claim. หลักฐานนี้ไม่ใช่ข้ออ้างว่า requirements ทั้งหมดเสร็จ และไม่เปลี่ยน Obsidian-only runtime guard หรือ recovery refs

### Conflict resolution policy — 2026-08-26

เมื่อเอกสารเก่ากับเอกสารใหม่ขัดกัน ให้ใช้ลำดับน้ำหนักดังนี้: (1) ข้อกำหนดล่าสุดของเจ้าของที่ผ่านการคำนวณและไม่ทำลาย performance/ความปลอดภัย, (2) runtime source, tests และ browser/device evidence ที่ตรวจได้จริง, (3) `GAME_RULES.md`/architecture contracts ที่ยังไม่ขัดกับข้อ 1–2, (4) Toolkit Master และ uploaded specifications ในฐานะ design requirements สำหรับงานถัดไป, และ (5) prototype/planning documents เก่าในฐานะประวัติและ reference เท่านั้น

ผลการตัดสินใจที่ใช้ต่อจากนี้คือใช้ **500m map radius** และ runtime scope แบบ **Obsidian-only** แทนข้อความเก่า 1,000–1,500m/การเปิด MAP_002–010 เพราะสอดคล้องกับข้อจำกัด performance และมี runtime guard จริง; ใช้ global render presets `near/balanced/far` เป็น compatibility contract ไปก่อน โดยยังไม่อ้างว่าเท่ากับ in-map view distance 5–50 blocks แบบละเอียด; แยก world map data จำนวน 100 records ออกจาก playable runtime; และถือ checklist `[x]` ใน adopted planning docs เป็น design history จนกว่าจะมี source/test/browser evidence ของเส้นทางนั้น

การเลือกค่าที่ลดภาระเครื่องเป็น **decision ด้าน product/performance** ไม่ใช่ผล benchmark บนอุปกรณ์จริง หากมีข้อกำหนดใหม่ที่ดีกว่าและมีหลักฐานรองรับ ให้เพิ่ม decision record และ impact review ก่อนเปลี่ยน contract ทุกครั้ง

### Checkpoint 2026-08-26 — Common Generator API foundation

เพิ่ม `server/generators/commonGeneratorApi.ts` เป็น backend/data-only foundation และ `server/commonGeneratorApi.test.ts` ครอบคลุม registry/version, deterministic generation/hash, preview, asset provenance validation, save/export และ tamper rejection. Artifact ใช้ schema `a-survival.generator-artifact.v1`, SHA-256 content hash ที่ไม่รวมเวลาสร้าง/บันทึก และไม่มี route หรือ UI ให้ผู้เล่นเรียกใช้. `pnpm check`, full suite `41` test files / `135` tests และ `NODE_OPTIONS=--max-old-space-size=1536 pnpm build` ผ่าน; build ยังมี analytics placeholder และ Babylon vendor chunk warning เดิม. generator รายด้าน, durable content storage, orchestrator, runtime import และ no-code platform ยังไม่เสร็จ

### Checkpoint 2026-08-26 — Structure/Building Generator foundation

เพิ่ม `server/generators/structureGenerator.ts` และ `server/structureGenerator.test.ts` โดยใช้ Common Generator API เป็น shared artifact boundary. Blueprint library มีครบ `object`, `building`, `compound`, `settlement`, `landmark`; placement ตรวจ biome, terrain, climate, slope, water, space, road, settlement, population, support, entry, bounds และ occupied footprint; generation คืน score, repairs, child structures, NPC/mob spawn counts และ asset references แบบ deterministic. มีการลอง candidate สำรองเมื่อจุดแรกชน footprint และ reject เมื่อไม่มีจุดที่ผ่าน. `pnpm check`, focused Structure tests และ full suite ผ่านก่อน checkpoint; actual models/textures, terrain/road/interior solver, runtime map integration และ player-facing generator UI ยังไม่เสร็จ

### Checkpoint 2026-08-26 — Content Catalog Generator foundation

เพิ่ม `server/generators/contentCatalogGenerator.ts` และ `server/contentCatalogGenerator.test.ts` เป็น backend/data-only catalog generator รองรับ 10 categories ได้แก่ weapon 3 แบบ, plant, seed, material, furniture, decoration, structure และ tool โดย default สร้าง 300 definitions ต่อหมวด รวม 3,000 definitions พร้อม tier, stack/equippable, combat metadata สำหรับอาวุธ, soil affinity สำหรับ plant/seed, logical assetRefs และ provenance. รัน `server/generators/generateContentSnapshot.ts` แล้วเก็บ `server/generators/generated/content-catalog-min-300.json` ขนาดประมาณ 1.1MB; snapshot reuse test โหลดกลับและ validate hash `76bc5d5001a0153ce3007b1a909f181409a9ce8cf2c5fba2534e857bf7a0bcfa`. เพิ่ม future-library pack `client/public/assets/packs/a-survival-content-library-v0-1/` จำนวน 16 procedural starter PNG พร้อม manifest/digest/provenance แต่ยังไม่ครอบคลุม definitions ทั้ง 3,000 รายการ. `pnpm check`, focused tests, full suite `45` test files / `154` tests และ production build ผ่าน; build มี analytics placeholder และ Babylon vendor chunk warning เดิม. ยังไม่มี final texture/model/atlas coverage, asset upload ภายนอก, universal balance/ecology/combat engine, runtime catalog migration หรือ player-facing generator UI

### Checkpoint 2026-08-26 — Universal Item Engine foundation

เพิ่ม `server/generators/universalItemEngine.ts` และ `server/universalItemEngine.test.ts` เป็น backend/data-only plugin `item.universal` บน Common Generator Registry. Definition บังคับ purpose, identity, role, material/environment tags, progression, counters, resource links, repair profile, durability, effects/counter/stack cap, compatibility, recommended builds, performance cost และ balance profile. Validator ตรวจ family/role coherence, multi-axis power creep, trade-off, power budget, effect caps และ world-resource relationship; ยังไม่เชื่อม crafting/equipment/combat/economy transaction, armor/set/enchant/socket/upgrade หรือ runtime catalog migration. `pnpm check`, full suite `46` test files / `159` tests และ production build ผ่าน; build ยังมี analytics placeholder และ Babylon vendor chunk warning เดิม


### Checkpoint 2026-08-27 — recovered Obsidian runtime/content foundation

หลังพบว่า local workspace อยู่บน `0a9586f` ขณะที่ remote มี creator/toolchain chain แยกอยู่ จึงเก็บงาน runtime/content ที่ยัง uncommitted เป็น local recovery commit `46a4812` แล้วรวมกับ `origin/main` แบบ non-destructive เป็น merge commit `6643883`. ชุดที่กู้คืนประกอบด้วย block-first records/actions/physics, plant/farming/storage/camera foundations, deterministic Obsidian world export seed `9107`, content generator/registry tools, AI-NPC service boundary และ player-facing Codex/Credits/language foundationsตามไฟล์จริง. ก่อน merge รัน `pnpm check`, full suite `46` files / `162` tests และ production build ด้วย memory cap ผ่าน. หน่วยนี้เป็น recovery/runtime foundation และยังไม่ยกระดับข้อกำหนดกว้างจาก `PARTIAL`; mobile/device acceptance, universal ecology/animation/performance และ story progression ยังไม่เสร็จ.

การ merge คง `RUNTIME_MAP_ID = "obsidian-frontier"`, ไม่เปิด future map runtime/cache, คง creator route เป็น developer-only และคง `creator` กับ `aiNpc` เป็น router siblings แยกจาก `game`. Recovery branch `local-recovery-46a4812` และ forensic snapshots ถูกเก็บไว้โดยไม่มี force push หรือ destructive Git operation.

### Checkpoint 2026-08-27 — protected creator and artifact foundations

หลัง recovery merge ได้ส่งหน่วย creator ต่อเนื่องแบบ non-force ได้แก่ `30a4b64` สำหรับ admin gate ของ Creator Studio, `304054b` สำหรับ durable texture artifact registry, `1fc27f9` สำหรับ protected deterministic previews ของ world/structure/item/weapon, `3c09ba9` สำหรับ Thai Creator Domain Workbench และ `46fccc0` สำหรับ register/list texture artifact ใน Creator Studio. ทุกหน่วยคง `creator` เป็น router sibling ของ `game`, ใช้ `adminProcedure` ที่ backend และไม่เพิ่มปุ่ม generator ใน player HUD. Browser session ปัจจุบันยืนยัน deny boundary ของ `/creator-studio` และ `/creator-workbench` เมื่อไม่มี admin session เท่านั้น จึงยังไม่อ้าง authenticated browser register/preview success หรือ runtime publish.

### Checkpoint 2026-08-27 — Obsidian block creator preview

`f73d8d8` เพิ่ม block panel ใน `CreatorDomainWorkbench` และ `creator.block.preview` ที่อ่าน canonical `OBSIDIAN_BLOCKS`/block definition registry, แสดง collision/tool/drop/support/hazard metadata และคืน `runtimeImportAllowed: false`. Focused block/structure/creator tests `4` files / `29` tests, full suite `60` files / `238` tests, `pnpm check`, production build และ diff-check ผ่าน. หน่วยนี้ยืนยันเฉพาะ no-code preview ของ canonical Obsidian block; ยังไม่เพิ่ม block เข้า runtime registry โดยอัตโนมัติ และยังไม่ทำ universal tree/leaf/gravity registry ครบ.

### Checkpoint 2026-08-27 — plant catalog relationship repair

`43dc917` รวม world-farm generator view เข้ากับ `PLANT_CATALOG`/`PLANT_ITEMS` runtime source เดียวกันครบ `300` รายการ. `seed-###` และ `world-plant-###` รุ่นเก่ายัง resolve ได้ แต่ normalized state จะบันทึก canonical `seed-plant-###`/`plant-###`; reward ใช้ `yieldItemId` ของ runtime catalog จริง. Focused plant/farm tests ผ่าน `4` files / `17` tests และ full suite ก่อน checkpoint ผ่าน `60` files / `238` tests พร้อม `pnpm check` และ production build. หน่วยนี้ไม่อ้างว่า plant art ครบ 300 ชนิด, distribution ทุก biome หรือ universal ecology engine เสร็จ.

### Checkpoint 2026-08-27 — no-code animation profile tool

`c95a592` เพิ่ม `animation.profile` บน Common Generator Registry, CLI `pnpm animation:generate`, admin-only `creator.animation.preview` และ panel ภาษาไทยใน Creator Domain Workbench. Profile สร้าง state `idle/walk/run/dash/attack/hurt/dead`, validate one-shot/dead constraints, เก็บ asset source/provenance ใน preview และมี policy `generateOnLoad: false`, `distanceBasedUpdate: true`, `sleepWhenOffscreen: true`, `runtimeAssetReuse: true`. Focused animation/creator tests `3` files / `14` tests, CLI smoke สร้าง artifact `7` states พร้อม deterministic hash, full suite `61` files / `241` tests, `pnpm check`, production build และ diff-check ผ่าน. ยังไม่มี skeleton/motion library, seeded motion variation, wind, retargeting, animation LOD controller, clips/sprite sheets ครบทุก state หรือ real-device animation benchmark; สถานะ T-02 จึงเป็น `PARTIAL`.

### Checkpoint 2026-08-27 — story progression, local quest state and item detail UX

`454f0eb` เพิ่ม `quest.progression` เป็น Common Generator plugin และ CLI `pnpm story:generate`; default output มี 100 map records และ 2,000 quest records (20 ต่อ map), unique quest IDs/titles/story beats, rewards, sequential prerequisites และ `runtimeImportAllowed: false` สำหรับ map อนาคต. `creator.quest.preview` และ Workbench panel เป็น admin-only preview/summary ไม่ส่ง story generation เข้า player runtime. Tests ยืนยัน count, chain, determinism และ future-map rejection; full suite ณ checkpoint ผ่าน `62` files / `244` tests และ production build ผ่าน.

`e0b561c` เพิ่ม `storyProgressionSystem` และ `LocalGameSession.storyProgress` แบบ local-first. Normalization รับเฉพาะ quest chain ของ Obsidian map 1, ปฏิเสธ out-of-order/duplicate/future quest และหลังครบ 20 เควสจะรายงาน `nextMapReadyIndex: 2` โดยไม่ทำให้ `getRuntimeStoryMapId(2)` กลายเป็น playable. `ITEM_DETAIL_AND_STORY_PROGRESS.md` อธิบาย boundary นี้.

หน่วย item detail เพิ่มใน working tree หลัง checkpoint `e0b561c` และผ่าน automated validation ล่าสุด `64` files / `250` tests, `pnpm check`, production build และ diff check ก่อน browser smoke. `Item Detail` ใช้ long-press `3.5` วินาทีบน Vault item และ short hotbar detail จาก canonical definition/provenance; browser evidence ใน `docs/item-detail-browser-evidence-2026-08-27.md` ยืนยัน landing/lobby/map selector/game HUD, long-press dialog และการไม่มี creator controls ใน player. ยังไม่อ้าง mobile-device acceptance หรือ quest objective/reward runtime ครบ.


### Checkpoint 2026-08-28 — B-06 inventory capacity audit

PR #35 ของ AI-2 ถูกตรวจใน detached worktree และ merge เข้า `origin/main` เป็น `9195b6b` จาก implementation `36f14422278ceedf9b375a20bf633362e54488e8`. ไฟล์ใหม่ `server/generators/inventoryCapacityDependencyGraph.ts` ใช้ canonical `PLAYER_INVENTORY_SLOTS=40`, `WORLD_STORAGE_DEFAULT_SLOTS=27`, block definitions และ `addItemToContainer` เพื่อทำ pure bounded preview; ไม่มี inventory/storage mutation, IndexedDB/DB write หรือ runtime caller integration. Focused B-06 suite ผ่าน `1` file / `5` tests และตรวจ canonical 40-slot, normal block stack 64, same-definition merge `63+1`, overflow `65 → 64 + remainder 1`, non-stackable เมื่อเต็ม 40 ช่อง และ required blocker `cross-map-carry-owner-missing`; full worker validation ผ่าน `118` files / `488` tests, `pnpm check`, `git diff --check` และ heap-limited production build. รายงานอยู่ที่ `docs/AI_HANDOFF_B06_INVENTORY_CAPACITY_REPORT.md`. Claim state ของ checkpoint เป็น `DONE` แต่ broad B-06 ยังคง `PARTIAL` เพราะยังไม่มี authoritative 40-slot enforcement ทั่วทุก reward/action, profile/session cross-map carry, persistence/reload/reconnect และ device acceptance.

การ merge ยังคง `RUNTIME_MAP_ID = "obsidian-frontier"`, ไม่มี future-map playable/cache/offline write และไม่มี creator/player UI change. Known build warnings เดิมเกี่ยวกับ analytics placeholders, analytics script module type, Babylon vendor chunk และ pnpm package configuration ไม่ได้อยู่ในขอบเขต B-06.

### Checkpoint 2026-08-28 — B-06 inventory input guard

AI-0 implementation `ee2126e` ถูก commit และ push ไป `origin/main` หลังแก้เฉพาะ `client/src/game/systems/inventorySystem.ts` และ `server/blockInventory.test.ts`. `addItemToContainer` ปฏิเสธ incoming quantity ที่ไม่ finite, ไม่เป็นจำนวนเต็ม, ไม่เป็นบวก และ capacity ที่ไม่ถูกต้อง ก่อนทำ merge/slot allocation; ไม่เปลี่ยน map, persistence, UI หรือ cross-map owner. Full test command ผ่าน `128` files / `547` tests และ `pnpm check` ผ่าน; `git diff --check` ผ่าน. `pnpm build` ผ่าน Vite transform แต่ถูกยุติด้วย exit `143` ระหว่าง rendering เพราะ sandbox มี memory pressure สูง จึงยังไม่มี production-build acceptance สำหรับ checkpoint นี้. Broad B-06 ยังคง `PARTIAL` ตาม owner requirements.


### Checkpoint 2026-08-28 — B-07 world storage isolation audit

PR #37 ของ AI-2 ถูกตรวจใน detached worktree และ merge เข้า `origin/main` เป็น `9d5d431` จาก implementation `02830c85d02886f77177fa8e5d7a0d42c6366a17`. ไฟล์ใหม่ `server/generators/worldStorageIsolationDependencyGraph.ts` อ่าน canonical `worldStorageSystem.ts`, `OfflineMapState`/storage owner, `CARRY_SLOT_LIMIT=40`, `CHEST_SLOT_LIMIT=27`, `worldStorageById` และ Obsidian anchor เพื่อทำ pure bounded audit; ไม่มี storage mutation, IndexedDB/DB write, network call หรือ UI/runtime integration. Focused B-07 suite ผ่าน `1` file / `5` tests และตรวจ chest/carry separation, map+player namespace, map-scoped transfer action, duplicate normalization, future-map fail-closed และ required blocker `universal-world-storage-integration-owner-missing`; full worker validation ผ่าน `118` files / `488` tests, `pnpm check`, `git diff --check` และ heap-limited production build. รายงานอยู่ที่ `docs/AI_HANDOFF_B07_WORLD_STORAGE_REPORT.md`. Claim state ของ checkpoint เป็น `DONE` แต่ broad B-07 ยังคง `PARTIAL` เพราะยังไม่มี universal world-state integration, full cross-map storage, authenticated sync/conflict resolution, production persistence และ device acceptance.

การ merge ยังคง `RUNTIME_MAP_ID = "obsidian-frontier"`, ไม่มี future-map playable/cache/offline write และไม่มี creator/player UI change. Known build warnings เดิมเกี่ยวกับ analytics placeholders, analytics script module type, Babylon vendor chunk และ pnpm package configuration ไม่ได้อยู่ในขอบเขต B-07.


### Checkpoint 2026-08-28 — S-04 adaptive performance audit

PR #38 ของ AI-2 ถูกตรวจใน detached worktree และ merge เข้า `origin/main` เป็น `731f25f` จาก implementation `f59042b4091e0f73e287beafb135144aef1763b7`. ไฟล์ใหม่ `server/generators/adaptivePerformanceDependencyGraph.ts` เชื่อม source จริงของ one-time capability advice, static performance budgets, render-distance/visibility, bounded telemetry และ preview-only profiler ใน pure dependency graph; graph ระบุ required blockers สำหรับ adaptive tier controller, runtime LOD, hysteresis, pooling และ sleep/wake. Focused S-04 suite ผ่าน `1` file / `5` tests และ full worker validation ผ่าน `118` files / `488` tests, `pnpm check`, `git diff --check` และ heap-limited production build. รายงานอยู่ที่ `docs/AI_HANDOFF_S04_ADAPTIVE_PERFORMANCE_REPORT.md`. Claim state ของ checkpoint เป็น `DONE` แต่ broad S-04 ยังคง `PARTIAL` เพราะยังไม่มี adaptive runtime mutation, reliable device/WebGL capability acceptance, runtime LOD consumer, hysteresis/pooling/sleep-wake owners, device/GPU benchmark หรือ mobile/WebView acceptance.

การ merge ยังคง generate-once/store/cache/reuse boundary, ไม่เพิ่มงานใน render loop, ไม่เปิด player profiler/generator UI และคง `RUNTIME_MAP_ID = "obsidian-frontier"` โดยไม่มี future-map playable/cache/offline write. Known build warnings เดิมเกี่ยวกับ analytics placeholders, analytics script module type, Babylon vendor chunk และ pnpm package configuration ไม่ได้อยู่ในขอบเขต S-04.


### Checkpoint 2026-08-28 — T-02 procedural animation/motion audit

PR #39 ของ AI-2 ถูกตรวจใน detached worktree และ merge เข้า `origin/main` เป็น `35e6c9a` จาก implementation `7572d942620d6fc84d70e6b87b3c66f62b077092`. ไฟล์ใหม่ `server/generators/animationMotionDependencyGraph.ts` เชื่อม canonical seven-state animation profile, bounded FPS/profile validation, pure full/reduced/sleep/static motion policy, reduced-motion behavior และ asset provenance/reference-only boundary แบบ read-only; graph ระบุ required blockers สำหรับ runtime caller/variation, skeleton retarget, wind และ binary animation asset generation. Focused T-02 suite ผ่าน `1` file / `5` tests และ full worker validation ผ่าน `118` files / `488` tests, `pnpm check`, `git diff --check` และ heap-limited production build. รายงานอยู่ที่ `docs/AI_HANDOFF_T02_ANIMATION_MOTION_REPORT.md`. Claim state ของ checkpoint เป็น `DONE` แต่ broad T-02 ยังคง `PARTIAL` เพราะยังไม่มี motion/skeleton library ที่ครบ, runtime variation/retarget/wind, binary asset generation, runtime caller integration, visual/device acceptance หรือ mobile/WebView acceptance.

การ merge ยังคง generate-once/store/cache/reuse boundary, ไม่มี binary asset ใหม่หรือ animation generation ใน render loop, ไม่มี player/creator UI change และคง `RUNTIME_MAP_ID = "obsidian-frontier"` โดยไม่มี future-map playable/cache/offline write. Known build warnings เดิมเกี่ยวกับ analytics placeholders, analytics script module type, Babylon vendor chunk และ pnpm package configuration ไม่ได้อยู่ในขอบเขต T-02.


### Checkpoint 2026-08-28 — G-01 deterministic world-generator coverage audit

PR #41 ของ AI-2 ถูกตรวจใน detached worktree และ merge เข้า `origin/main` เป็น `c72c5d1` จาก implementation `f1b54183f4055b56e209354a5c3463b183f1084c`. ไฟล์ใหม่ `server/generators/worldGeneratorCoverageDependencyGraph.ts` เรียก canonical `tools/world-generator.ts` แบบ in-memory บน `obsidian-frontier` และตรวจ terrain/water/tree/vegetation/resources/caves/structures/spawn roles, deterministic repeat hash, real `validateGeneratedWorld` result และ no-write module export preview. Focused G-01 suite ผ่าน `1` file / `5` tests และ full worker validation ผ่าน `118` files / `488` tests, `pnpm check`, `git diff --check` และ heap-limited production build. รายงานอยู่ที่ `docs/AI_HANDOFF_G01_WORLD_GENERATOR_REPORT.md`. Claim state ของ checkpoint เป็น `DONE` แต่ broad G-01 ยังคง `PARTIAL` เพราะยังไม่มี universal/world-wide output acceptance, persisted export บน disk, asset-manifest binding/publish, runtime loading, live NPC/boss behavior, player-facing generator browser acceptance หรือ device acceptance.

การ merge ยังคง backend-only/player-facing-generator-disabled policy, ไม่เปิด future map, cache หรือ offline write และไม่มี binary asset generation. Known build warnings เดิมเกี่ยวกับ analytics placeholders, analytics script module type, Babylon vendor chunk และ pnpm package configuration ไม่ได้อยู่ในขอบเขต G-01.


### Checkpoint 2026-08-28 — G-06 AI NPC service timeout hardening

AI-0 ตรวจ source จริงของ `server/aiNpcService.ts`, `server/aiNpcService.test.ts` และ caller `server/routers.ts` แล้วพบว่า canonical service/provider boundary มีอยู่ก่อน checkpoint นี้ จึงไม่สร้าง service ซ้ำ. Implementation commit `738f01f` แก้ timeout handle ให้อยู่ใน scope ที่ `finally` ล้างได้ทุกเส้นทาง ทั้ง provider success, HTTP error, invalid output และ abort/error fallback; เพิ่ม regression test ด้วย fake timers เพื่อยืนยันว่า provider abort แล้ว fallback อย่างปลอดภัยและไม่มี timer ค้าง. Focused suite ผ่าน `1` file / `7` tests, full suite ผ่าน `127` files / `537` tests, `pnpm check`, `git diff --check` และ heap-limited production build.

หลักฐานนี้ทำให้ G-06 ขยับจาก `PENDING` เป็น `PARTIAL` ใน broad matrix แต่ไม่ได้ปิดทั้งข้อ. ระบบยังไม่มี in-game NPC/encounter binding ที่ตรวจยืนยันได้, authoritative action executor, rate-limit/auth abuse policy, provider/production configuration, persistence, device acceptance หรือหลักฐานการเล่นจริง. Provider call ยังคงอยู่ฝั่ง server, default disabled เมื่อไม่มี explicit flag และ server key, จำกัดเฉพาะ `obsidian-frontier:special-ai`, ไม่เปิด future map, ไม่สร้าง background loop และไม่อ้างว่า live AI NPC พร้อมใช้งานครบระบบ.


### Checkpoint 2026-08-28 — M-03 mapCache payload-integrity boundary

AI-0 ตรวจ current `mapCache.ts`, `mapCache.test.ts` และแยกขอบเขตจาก PR #46 ของ AI-3 ซึ่งอ้าง `client/public/sw.js` และ service-worker test ของตนเอง. Implementation commit `2ca2527` แก้เฉพาะ `client/src/game/storage/mapCache.ts` และ `server/mapCache.test.ts`: cache hit ต้องมี Cache Storage response ที่ parse ได้และตรงกับ canonical `MapDefinition` ทั้ง `id`, `name`, `keyArt`, `content` และ `cachedAt` ที่เป็นค่าบวก; stale/corrupt payload จะไม่ถูกนับเป็น cache ที่พร้อมเปิดออฟไลน์. การตรวจยังคงปฏิเสธ future-map ก่อน cache/network work และไม่ใช้ localStorage marker เป็นหลักฐาน integrity แทน payload จริง.

Focused M-03 suite ผ่าน `1` file / `5` tests, full suite ผ่าน `127` files / `538` tests, `pnpm check`, `git diff --check` และ heap-limited production build. Checkpoint นี้เป็น mapCache integrity sub-scope เท่านั้น ยังไม่อ้าง service-worker route, digest/version migration, airplane-mode/reconnect/resync, map/chest/plant persistence, real-device หรือ WebView acceptance; PR #46 ยังคงเป็น worker contribution ที่ต้อง review แยก.


### Checkpoint 2026-08-28 — B-01 grouped block unique-coordinate runtime fix

AI-0 ตรวจ `client/src/game/systems/blockWorldSystem.ts` และพบช่องว่างใน canonical runtime owner: `generateBlockGroup` สร้างทุก offset แล้วสร้าง `occupied` จาก records ทั้งหมดภายหลัง ทำให้ filter เดิมไม่ตัด duplicate `blockKey`. Implementation commit `a893d6f` เปลี่ยนให้ใช้ `addIfEmpty` ระหว่างเดิน offsets จึงคง record แรกเมื่อพิกัดที่ผ่านการปัดเศษซ้ำกัน และคืน output ที่มี unique keys ตามลำดับ deterministic; ไม่แตะ physics, inventory, persistence, map selector, UI หรือ pure graph files ของ PR #44/#49.

เพิ่ม `server/blockWorldSystem.test.ts` สำหรับ first-wins duplicate coordinate และ deterministic grouped output. Focused suite ผ่าน `1` file / `2` tests, full suite ผ่าน `128` files / `540` tests, `pnpm check`, `git diff --check` และ heap-limited production build. หลักฐานนี้ปิดเฉพาะ bug ของ grouped runtime generation และทำให้ B-01 ยังคง `PARTIAL`; ยังไม่มี universal block-family coverage, authoritative mutation/persistence/inventory/physics integration หรือ browser/device acceptance.


### Checkpoint 2026-08-28 — offline map override known-block validation

AI-0 ตรวจ `client/src/game/storage/indexedDb.ts` และพบว่า `normalizeWorldBlockOverrideMap` เดิมตรวจเพียงรูปแบบ key กับชนิด `string|null` จึงปล่อย unknown block-module strings ผ่าน offline state normalization ได้. Implementation commit `3c1d4d8` เพิ่มการอ้าง canonical `getBlockDefinition`: เก็บเฉพาะ integer coordinate keys ที่ชี้ไปยัง block definition ที่มีอยู่จริงหรือเป็น null tombstone และตัด unknown/malformed values ก่อน state ถูกส่งต่อไปยัง save/load owner. Future-map write rejection และ composite `[mapId+playerId]` namespace ไม่ถูกเปลี่ยน.

เพิ่ม regression ใน `server/offlineMapState.test.ts`; focused suite ผ่าน `1` file / `5` tests, full suite ผ่าน `128` files / `541` tests, `pnpm check`, `git diff --check` และ heap-limited production build. Checkpoint นี้เป็น validation ของ offline override input เท่านั้น ยังไม่อ้างว่า authoritative persistence, reconnect/resync, conflict resolution, digest/version migration, cross-device หรือ device acceptance เสร็จ.


### Checkpoint 2026-08-28 — T-07 quest reward quantity fail-closed guard

AI-0 ตรวจ `client/src/game/systems/questRewardDispatchSystem.ts` และ canonical `createMapRewardInstance`/`addItemToContainer` แล้วพบว่าการ clamp quantity เดิมอาจทำให้ input reward ที่เป็นศูนย์, ติดลบ, เศษส่วน, `NaN`, `Infinity` หรือเกิน stack limit ถูกเปลี่ยนความหมายหรือไหลไปถึง inventory path. Implementation commit `1e1b177` เพิ่ม `invalid-reward-quantity` และ reject explicit non-finite, non-integer, non-positive หรือเกิน `ItemDefinition.stackLimit` ก่อนสร้าง reward instance; transition ที่ยอมรับยังคง item-only, atomic และ caller-owned persistence.

เพิ่ม regression ใน `server/questRewardDispatchSystem.test.ts` ครอบคลุม quantity ผิดรูปแบบและยืนยันว่า state/inventory/discovery ไม่ถูก mutate. Focused suite ผ่าน `1` file / `9` tests, full suite ผ่าน `128` files / `542` tests, `pnpm check`, `git diff --check` และ heap-limited production build. Checkpoint นี้เป็น input-safety hardening เท่านั้น ไม่ได้สร้าง reward, ability หรือ reputation owner, ไม่ได้ dispatch gameplay event, ไม่ได้เปิด future map และไม่ปิด T-07 broad requirement ซึ่งยัง `PARTIAL`.


### Checkpoint 2026-08-28 — B-01 coordinate-record normalization

AI-0 ตรวจ canonical `createWorldBlock` ใน `client/src/game/systems/blockWorldSystem.ts` และพบว่าเดิม `blockKey` ปัดพิกัดด้วย `Math.round` แต่ fields `x/y/z` เก็บค่าทศนิยมเดิม ทำให้ record อาจไม่สอดคล้องกับ key. Implementation commit `dc4e47f` ปัด x/y/z ครั้งเดียวก่อนเขียนทั้ง fields และ key; ไม่เปลี่ยน map allow-list, persistence, physics, inventory หรือ UI. Regression ใน `server/blockWorldSystem.test.ts` ยืนยัน fractional offsets ให้ fields ตรงกับ rounded key และยังคง first-wins/deterministic grouped output.

Focused suite ผ่าน `1` file / `3` tests, full suite ผ่าน `128` files / `543` tests, `pnpm check`, `git diff --check` และ heap-limited production build. หลักฐานนี้แก้เฉพาะ coordinate-record consistency; B-01 ยังคง `PARTIAL` เพราะยังขาด universal block-family records, authoritative mutation/persistence/inventory/physics integration และ browser/device acceptance.


### Checkpoint 2026-08-28 — O-04 generator asset-reference gate

AI-0 ตรวจ `CommonGeneratorRegistry.generate` ใน `server/generators/commonGeneratorApi.ts` และพบว่า plugin output validation ผ่านแล้ว แต่ asset references จาก `preview` ยังไม่ถูกตรวจจนกว่าจะเรียก validate/save/export. Implementation commit `cc7bc85` เพิ่ม generation-time validation ของ `GeneratorAssetRef[]`; invalid SHA-256, `reference-only` ที่ไม่มี provenance และ duplicate asset IDs จะ fail closed ด้วย `GeneratorValidationError` ก่อน artifact ถูกส่งต่อไปยัง store/reuse path. ไม่มีการเปิด generator ใน player runtime และไม่เปลี่ยน generate-once/store/cache/reuse semantics.

เพิ่ม regression ใน `server/commonGeneratorApi.test.ts`; focused suite ผ่าน `1` file / `7` tests, full suite ผ่าน `128` files / `545` tests, `pnpm check`, `git diff --check` และ heap-limited production build. หลักฐานนี้ลด O-04 จาก `PENDING` เป็น `PARTIAL` เฉพาะ asset-reference gate; ยังขาด tool inventory/dependency policy ครบทุก domain, durable registry/orchestrator/export/review และการพิสูจน์การใช้งานเครื่องมือจริง. PR #28 เป็น worker scope แยกและยังไม่ merge.


### Checkpoint 2026-08-28 — F-03 harvest inventory-capacity boundary

AI-0 ตรวจ `harvestWorldPlant` ใน `client/src/game/systems/worldFarmSystem.ts` และพบว่า wrapper เดิมใช้ `input.inventory.concat(plan.reward)` จึง bypass `PLAYER_INVENTORY_SLOTS=40` และ item stack rules. Implementation commit `ee5bbbe` เปลี่ยนเป็น `addItemToContainer` แบบ bounded; เมื่อ carry เต็มหรือผลผลิตใส่ไม่ได้ ระบบจะคืน rejected plan พร้อม state/inventory เดิมและไม่ล้าง mature plot. เมื่อใส่ได้จึงคืน inventory ที่ผ่าน canonical merge/stack path. ไม่เพิ่ม plant/effect/reward owner และไม่เปลี่ยน map/persistence/UI contract.

เพิ่ม regression ใน `server/worldFarmSystem.test.ts`; focused suite ผ่าน `1` file / `6` tests, full suite ผ่าน `128` files / `546` tests, `pnpm check`, `git diff --check` และ heap-limited production build. หลักฐานนี้ปิดเฉพาะ harvest-to-carry capacity boundary; F-03 ยังคง `PARTIAL` เพราะ stage/profile coverage, authoritative persistence/reconnect, unloaded-chunk abstraction และ browser/device acceptance ยังไม่ครบ.
