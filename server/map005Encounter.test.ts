import { describe, expect, it } from "vitest";
import { MAP005_DRIZZLE_OFFSET_MS, MAP005_DRIZZLE_PERIOD_MS, MAP005_HYDRA_NEST, MAP005_TELEGRAPH_MS, MAP005_VANE_SHELTER, initialMap005Encounter, resolveMap005Encounter } from "../client/src/game/map005/encounter";
describe("MAP_005 Acid Drizzle encounter", () => { const drizzleNow = MAP005_DRIZZLE_PERIOD_MS - MAP005_DRIZZLE_OFFSET_MS;
  it("applies deterministic damage only outside shelter", () => { expect(resolveMap005Encounter(initialMap005Encounter(), { x: 0, z: 0, health: 100, harvestedLilies: 0, defeatedSlimes: 0, interacted: false, now: drizzleNow }).acidDamagePerSecond).toBe(6); expect(resolveMap005Encounter(initialMap005Encounter(), { x: MAP005_VANE_SHELTER.x, z: MAP005_VANE_SHELTER.z, health: 100, harvestedLilies: 0, defeatedSlimes: 0, interacted: false, now: drizzleNow }).acidDamagePerSecond).toBe(0); });
  it("requires elite progress and telegraph before persistent Hydra state", () => { const safeNow = 100_000; const elite = resolveMap005Encounter(initialMap005Encounter(), { x: 0, z: 0, health: 100, harvestedLilies: 3, defeatedSlimes: 0, interacted: false, now: safeNow }); const telegraph = resolveMap005Encounter(elite.memory, { x: MAP005_HYDRA_NEST.x, z: MAP005_HYDRA_NEST.z, health: 100, harvestedLilies: 3, defeatedSlimes: 0, interacted: true, now: safeNow }); const active = resolveMap005Encounter(telegraph.memory, { x: 0, z: 0, health: 100, harvestedLilies: 3, defeatedSlimes: 0, interacted: false, now: safeNow + MAP005_TELEGRAPH_MS }); expect(active.activateBoss).toBe(true); });
});
