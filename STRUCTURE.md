# A_Survival — โครงสร้างที่วางไว้

ชั้นหน้าจอจะรับผิดชอบเฉพาะประสบการณ์ผู้ใช้ ได้แก่ เลือกโปรไฟล์ สร้างหรือเข้าห้อง รับคำขอ และแสดงสถานะการเชื่อมต่อ ส่วนตรรกะเกมและข้อมูลจะไม่ผูกกับหน้าจอ เพื่อให้เปลี่ยนวิธีการเชื่อมต่อจากเครือข่าย Wi‑Fi ในพื้นที่ไปเป็นออนไลน์ภายหลังได้โดยไม่ต้องเขียนกติกาเกมใหม่

| โมดูล | หน้าที่ | หลักการข้อมูล |
|---|---|---|
| Local Profile | สร้าง เลือก และบันทึกตัวละครเจ้าของเครื่อง | ไม่ส่งข้อมูลถาวรทั้งก้อนไปเขียนทับเครื่องอื่น |
| Room Session | สร้างห้อง เลือกแผนที่ ขอและตอบรับการเชื่อมต่อ | ทุกคำขอต้องมีสถานะและหมดอายุได้ |
| Sync Preparation | เปรียบเทียบข้อมูลที่จำเป็นและแสดงเปอร์เซ็นต์การเตรียมพร้อม | แชร์เฉพาะข้อมูลเพื่อเล่นร่วมกัน |
| Shared World State | สถานะผู้เล่นและไอเทมในห้องระหว่างเล่น | มีเจ้าของการตัดสินชัดเจนและตรวจลำดับเหตุการณ์ |
| Transport | ส่ง/รับข้อความภายใน Wi‑Fi หรือออนไลน์ในอนาคต | ใช้ interface เดียวกันในเกม |

## Runtime modules

| Path | เจ้าของความรับผิดชอบ | ข้อมูลที่จัดการ |
|---|---|---|
| `src/game/localProfileStore.ts` | สร้าง เลือก และเก็บ Local Player Profile ในอุปกรณ์ | ข้อมูลถาวรของตัวละครเจ้าของเครื่องเท่านั้น |
| `src/game/models.ts` | นิยามข้อมูล profile, room และ map ที่ใช้ร่วมกัน | ไม่มีการเชื่อมต่อหรือ side effect |
| `src/game/lan-protocol.ts` | นิยามข้อความ create, request, accept/reject และ session | ข้อมูลที่ส่งผ่าน transport เท่านั้น |
| `src/game/lan-transport.ts` | Local LAN WebSocket client | ไม่ตัดสินกติกาเกมและสลับ transport ได้ภายหลัง |
| `server/lan-host.mjs` | เจ้าของห้องในเครือข่ายท้องถิ่น | room session, join request และสมาชิกที่ยืนยันแล้ว |
| `src/App.tsx` | UI profile, lobby, request/accept และ sync progress | แสดงผลตาม protocol โดยไม่บันทึก profile ของผู้อื่น |
| `src/components/GameCanvas.tsx` | ฉาก Babylon ทดสอบและ lifecycle ของ canvas | render-only; dispose ทุก resource เมื่อออกจากฉาก |
